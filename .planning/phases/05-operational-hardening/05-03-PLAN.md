---
phase: 05-operational-hardening
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - hub/dashboard/src/lib/types.ts
  - hub/dashboard/src/lib/CalibrationStatusBadge.svelte
  - hub/dashboard/src/lib/CalibrationStatusBadge.test.ts
  - hub/dashboard/src/lib/NtfySettingsForm.svelte
  - hub/dashboard/src/lib/NtfySettingsForm.test.ts
  - hub/dashboard/src/lib/StoragePanel.svelte
  - hub/dashboard/src/lib/StoragePanel.test.ts
  - hub/dashboard/src/routes/zones/[id]/+page.svelte
  - hub/dashboard/src/routes/settings/calibration/+page.svelte
  - hub/dashboard/src/routes/settings/notifications/+page.svelte
  - hub/dashboard/src/routes/settings/storage/+page.svelte
  - hub/dashboard/src/routes/+layout.svelte
autonomous: true
requirements: [ZONE-07, NOTF-03]

must_haves:
  truths:
    - "Calibration management page at /settings/calibration lists all pH sensors with overdue badges"
    - "Farmer can record calibration from the calibration settings page with a single button tap"
    - "Zone detail page shows inline pH calibration action when pH sensor exists"
    - "ntfy settings page at /settings/notifications has URL, topic, toggle, and Send Test button"
    - "Storage page at /settings/storage shows per-table sizes and Purge Now with confirmation"
    - "Settings navigation links appear in the layout for all three new settings pages"
  artifacts:
    - path: "hub/dashboard/src/lib/CalibrationStatusBadge.svelte"
      provides: "Pill badge showing calibration status (overdue, due soon, current)"
    - path: "hub/dashboard/src/lib/NtfySettingsForm.svelte"
      provides: "Form with URL, topic, toggle, save, and test button"
    - path: "hub/dashboard/src/lib/StoragePanel.svelte"
      provides: "Table of per-table sizes with purge button and confirmation"
    - path: "hub/dashboard/src/routes/settings/calibration/+page.svelte"
      provides: "Calibration management page listing all sensors"
    - path: "hub/dashboard/src/routes/settings/notifications/+page.svelte"
      provides: "ntfy notification settings page"
    - path: "hub/dashboard/src/routes/settings/storage/+page.svelte"
      provides: "Storage and retention settings page"
  key_links:
    - from: "hub/dashboard/src/routes/settings/calibration/+page.svelte"
      to: "/api/calibrations"
      via: "fetch GET on mount, POST on record"
      pattern: "fetch.*api/calibrations"
    - from: "hub/dashboard/src/routes/settings/notifications/+page.svelte"
      to: "/api/settings/notifications"
      via: "fetch GET on mount, PATCH on save, POST .../test"
      pattern: "fetch.*api/settings/notifications"
    - from: "hub/dashboard/src/routes/settings/storage/+page.svelte"
      to: "/api/storage/stats"
      via: "fetch GET on mount, POST /api/storage/purge"
      pattern: "fetch.*api/storage"
---

<objective>
All Phase 5 frontend: calibration management page, ntfy settings page, storage settings page, inline pH calibration action on zone detail, new TypeScript types, and settings navigation.

Purpose: Deliver the farmer-facing UI for pH calibration workflow (D-02, D-05), ntfy configuration (D-07), and storage management (D-12) following the established /settings/ai page pattern.

Output: Three new settings pages, inline zone detail action, CalibrationStatusBadge component, NtfySettingsForm component, StoragePanel component, component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operational-hardening/05-CONTEXT.md
@.planning/phases/05-operational-hardening/05-RESEARCH.md
@.planning/phases/05-operational-hardening/05-UI-SPEC.md
@.planning/phases/05-operational-hardening/05-01-SUMMARY.md
@.planning/phases/05-operational-hardening/05-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from codebase and prior plans -->

From hub/dashboard/src/lib/types.ts (existing types):
```typescript
export type AlertSeverity = 'P0' | 'P1';
export interface AlertEntry {
  key: string;
  severity: AlertSeverity;
  message: string;
  deep_link: string;
  count: number;
}
export type AIDomain = 'irrigation' | 'zone_health' | 'flock_anomaly';
export type AIMode = 'ai' | 'rules';
```

From hub/dashboard/src/routes/settings/ai/+page.svelte (settings page pattern):
```svelte
<div class="settings-page">
  <h2 class="settings-heading">AI Engine Settings</h2>
  <div class="settings-list">
    ...
  </div>
</div>

<style>
  .settings-page { padding: var(--spacing-md); }
  .settings-heading {
    font-size: 20px; font-weight: 600;
    font-family: 'Merriweather', Georgia, serif;
    color: var(--color-text-primary);
    line-height: 1.2; margin-bottom: var(--spacing-lg);
  }
  .settings-list { display: flex; flex-direction: column; gap: var(--spacing-lg); }
</style>
```

From hub/dashboard/src/lib/AISettingsToggle.svelte (toggle pattern):
```svelte
<!-- Uses role="switch", aria-checked, optimistic UI pattern -->
```

From hub/dashboard/src/routes/zones/[id]/+page.svelte (zone detail sensors section):
```svelte
<section class="sensors-section">
  {#if zone.ph}
    <SensorValue icon={FlaskConical} label="pH" value={zone.ph.value} unit="" quality={zone.ph.quality} />
  {/if}
</section>
```

API endpoints (from Plans 01 and 02):
- GET /api/calibrations -> [{zone_id, sensor_type, offset_value, dry_value, wet_value, temp_coefficient, last_calibration_date, days_since_calibration}, ...]
- POST /api/calibrations/{zone_id}/{sensor_type}/record -> body: {offset, dry_value, wet_value, temp_coefficient}
- PATCH /api/calibrations/{zone_id}/{sensor_type} -> body: {offset_value?, dry_value?, wet_value?, temp_coefficient?}
- GET /api/settings/notifications -> {url, topic, enabled}
- PATCH /api/settings/notifications -> body: {url?, topic?, enabled?}
- POST /api/settings/notifications/test -> {status: "ok"} or error
- GET /api/storage/stats -> {tables: [{tablename, size, size_bytes}], retention_policies: [...], total_size, total_bytes}
- POST /api/storage/purge -> {status: "ok", message: "Purge complete"}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, CalibrationStatusBadge, calibration settings page, and zone detail inline action</name>
  <files>
    hub/dashboard/src/lib/types.ts,
    hub/dashboard/src/lib/CalibrationStatusBadge.svelte,
    hub/dashboard/src/lib/CalibrationStatusBadge.test.ts,
    hub/dashboard/src/routes/settings/calibration/+page.svelte,
    hub/dashboard/src/routes/zones/[id]/+page.svelte
  </files>
  <read_first>
    hub/dashboard/src/lib/types.ts,
    hub/dashboard/src/lib/AISettingsToggle.svelte,
    hub/dashboard/src/routes/settings/ai/+page.svelte,
    hub/dashboard/src/routes/zones/[id]/+page.svelte,
    .planning/phases/05-operational-hardening/05-UI-SPEC.md
  </read_first>
  <behavior>
    - CalibrationStatusBadge renders "OVERDUE" with amber background when days_since > 14
    - CalibrationStatusBadge renders "Due in N days" when days_since is between 12 and 14
    - CalibrationStatusBadge renders "Calibrated N days ago" when days_since < 12
    - CalibrationStatusBadge renders "OVERDUE" when last_calibration_date is null (never calibrated)
  </behavior>
  <action>
    **TypeScript types (hub/dashboard/src/lib/types.ts):**

    Add Phase 5 types after the existing Phase 4 types section:

    ```typescript
    // Phase 5 types

    export interface CalibrationEntry {
      zone_id: string;
      sensor_type: string;
      offset_value: number;
      dry_value: number | null;
      wet_value: number | null;
      temp_coefficient: number;
      last_calibration_date: string | null; // ISO 8601 or null
      days_since_calibration: number | null;
    }

    export interface NtfySettings {
      url: string;
      topic: string;
      enabled: boolean;
    }

    export interface StorageTableInfo {
      tablename: string;
      size: string;
      size_bytes: number;
    }

    export interface StorageStats {
      tables: StorageTableInfo[];
      retention_policies: Array<{
        hypertable_name: string;
        schedule_interval: string;
        config: Record<string, unknown>;
      }>;
      total_size: string;
      total_bytes: number;
    }
    ```

    **CalibrationStatusBadge.svelte (per UI-SPEC Component Inventory):**

    Create `hub/dashboard/src/lib/CalibrationStatusBadge.svelte`:

    Props: `days_since: number | null` (null = never calibrated).

    Logic:
    - `null` or `> 14`: render "OVERDUE" — background `var(--color-stale)`, text `#0f1117`, 14px/400 Inter.
    - `12 to 14` (inclusive): render "Due in {14 - days_since} days" — same amber color (due soon).
    - `< 12`: render "Calibrated {days_since} days ago" — background `var(--color-border)`, text `var(--color-text-secondary)`, 14px/400 Inter.
    - Border-radius: 4px (matches existing quality-badge).
    - Padding: `2px var(--spacing-sm)` (matches count-badge pattern in AlertBar).

    **CalibrationStatusBadge.test.ts:**

    Use vitest + @testing-library/svelte (same pattern as existing tests):
    - `test_renders_overdue_when_null`: pass days_since=null, assert text "OVERDUE".
    - `test_renders_overdue_when_over_14`: pass days_since=16, assert text "OVERDUE".
    - `test_renders_due_soon_when_12_to_14`: pass days_since=13, assert text "Due in 1 days".
    - `test_renders_calibrated_when_recent`: pass days_since=5, assert text "Calibrated 5 days ago".

    **Calibration settings page (/settings/calibration/+page.svelte, per D-02, D-05, UI-SPEC CalibrationRow):**

    Create `hub/dashboard/src/routes/settings/calibration/+page.svelte`:

    - On mount: `fetch('/api/calibrations')` to load list of CalibrationEntry[].
    - Render with settings-page/settings-heading/settings-list pattern from /settings/ai.
    - Page title: "Calibration Management" (per UI-SPEC).
    - Each CalibrationEntry rendered as a CalibrationRow:
      - Flex row, align-items center, min-height 44px (touch target).
      - Left: zone name (16px/400), sensor type label (14px/400 muted).
      - Center: CalibrationStatusBadge with days_since_calibration.
      - Right: "Record Calibration" button.
        - When NOT overdue: outline style (`border: 1px solid var(--color-accent)`, `color: var(--color-accent)`, `background: transparent`).
        - When overdue: filled style (`background: var(--color-accent)`, `color: #0f1117`).
      - Divider: `border-bottom: 1px solid var(--color-border)` between rows.
    - "Record Calibration" button handler: POST to `/api/calibrations/{zone_id}/{sensor_type}/record` with body `{offset: 0.0}` (offset is computed by hub from buffer values per UI-SPEC Interaction Contract). On success: show toast "Calibration recorded for {zone_id} pH sensor" (exact copy from UI-SPEC). On failure: show toast "Calibration save failed -- check connection". Reload calibration list after success.
    - Empty state: "No pH sensors configured. Add a zone with a pH sensor to begin tracking calibration." (exact copy from UI-SPEC).
    - Filter to show only sensor_type === "ph" entries (calibration tracking is pH-specific per ZONE-07).

    Also show editable fields for D-05 (per-sensor dry/wet values, temp coefficient):
    - Below each CalibrationRow, show expandable detail section (toggled by clicking row):
      - offset_value: number input
      - dry_value: number input (nullable)
      - wet_value: number input (nullable)
      - temp_coefficient: number input
    - "Save" button: PATCH `/api/calibrations/{zone_id}/{sensor_type}` with changed fields.
    - Input styles per UI-SPEC NtfySettingsForm: `background: var(--color-bg)`, `border: 1px solid var(--color-border)`, `border-radius: var(--radius-sm)`, focus: `border-color: var(--color-accent)`.

    **Zone detail inline action (per D-02, UI-SPEC PhCalibrationInlineAction):**

    Extend `hub/dashboard/src/routes/zones/[id]/+page.svelte`:

    - After the pH SensorValue row in the sensors-section, add inline calibration action:
      ```svelte
      {#if zone.ph}
        <SensorValue icon={FlaskConical} label="pH" value={zone.ph.value} unit="" quality={zone.ph.quality} />
        <PhCalibrationInlineAction zoneId={zoneId} />
      {/if}
      ```
    - `PhCalibrationInlineAction` is a small inline component (can be defined in the same file or a separate component). It:
      - Fetches calibration status for this zone from `/api/calibrations` (filter by zone_id and sensor_type="ph").
      - Shows "Record Calibration" link-button: 14px/400 Inter, `color: var(--color-accent)`, no underline, min-height 44px tap target.
      - When overdue: accompanied by CalibrationStatusBadge "OVERDUE" inline.
      - On click: POST to `/api/calibrations/{zoneId}/ph/record` with `{offset: 0.0}`. Show toast on success/failure (same copy as calibration page).

    For simplicity, implement the inline action directly in the zone detail page file rather than a separate component (it is a small snippet of 15-20 lines of markup).

    Use `$state` for loading the calibration data on mount, and `$state` for the recording loading state. Fetch calibration data in an `$effect` that depends on `zoneId`.
  </action>
  <verify>
    <automated>cd /Users/patrick.lewis/code/backyard-farm/hub/dashboard && npx vitest run src/lib/CalibrationStatusBadge.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - hub/dashboard/src/lib/types.ts contains "export interface CalibrationEntry"
    - hub/dashboard/src/lib/types.ts contains "export interface NtfySettings"
    - hub/dashboard/src/lib/types.ts contains "export interface StorageStats"
    - hub/dashboard/src/lib/CalibrationStatusBadge.svelte contains "OVERDUE"
    - hub/dashboard/src/lib/CalibrationStatusBadge.svelte contains "var(--color-stale)"
    - hub/dashboard/src/lib/CalibrationStatusBadge.svelte contains "Calibrated"
    - hub/dashboard/src/routes/settings/calibration/+page.svelte contains "Calibration Management"
    - hub/dashboard/src/routes/settings/calibration/+page.svelte contains "api/calibrations"
    - hub/dashboard/src/routes/settings/calibration/+page.svelte contains "Record Calibration"
    - hub/dashboard/src/routes/settings/calibration/+page.svelte contains "Calibration recorded for"
    - hub/dashboard/src/routes/settings/calibration/+page.svelte contains "No pH sensors configured"
    - hub/dashboard/src/routes/zones/[id]/+page.svelte contains "Record Calibration"
    - hub/dashboard/src/routes/zones/[id]/+page.svelte contains "api/calibrations"
    - CalibrationStatusBadge.test.ts tests pass
  </acceptance_criteria>
  <done>Calibration settings page lists all pH sensors with status badges and record/edit capability. Zone detail page has inline calibration action for pH sensors. All copy matches UI-SPEC exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NtfySettingsForm, notifications page, StoragePanel, storage page, and settings nav</name>
  <files>
    hub/dashboard/src/lib/NtfySettingsForm.svelte,
    hub/dashboard/src/lib/NtfySettingsForm.test.ts,
    hub/dashboard/src/lib/StoragePanel.svelte,
    hub/dashboard/src/lib/StoragePanel.test.ts,
    hub/dashboard/src/routes/settings/notifications/+page.svelte,
    hub/dashboard/src/routes/settings/storage/+page.svelte,
    hub/dashboard/src/routes/+layout.svelte
  </files>
  <read_first>
    hub/dashboard/src/routes/settings/ai/+page.svelte,
    hub/dashboard/src/lib/AISettingsToggle.svelte,
    hub/dashboard/src/lib/CommandButton.svelte,
    hub/dashboard/src/routes/+layout.svelte,
    .planning/phases/05-operational-hardening/05-UI-SPEC.md
  </read_first>
  <behavior>
    - NtfySettingsForm renders URL input, topic input, enabled toggle, Save Settings button, Send Test button
    - NtfySettingsForm Send Test button shows "Sending..." during loading state
    - NtfySettingsForm disabled state shows empty state copy when URL is empty
    - StoragePanel renders table rows with table name and size
    - StoragePanel Purge Now button shows inline confirmation panel before executing
    - StoragePanel confirmation panel shows destructive warning text and two buttons
  </behavior>
  <action>
    **NtfySettingsForm.svelte (per UI-SPEC NtfySettingsForm component):**

    Create `hub/dashboard/src/lib/NtfySettingsForm.svelte`:

    Props: `settings: NtfySettings` (initial values from API), `onsave: (settings: NtfySettings) => Promise<void>`, `ontest: () => Promise<boolean>`.

    Internal state:
    - `let url = $state(settings.url)`, `let topic = $state(settings.topic)`, `let enabled = $state(settings.enabled)`.
    - `let testStatus = $state<'idle' | 'sending' | 'ok' | 'error'>('idle')`.
    - `let saveLoading = $state(false)`.

    Layout (per UI-SPEC):
    - Toggle switch for "Push Notifications" at top. Reuse the AISettingsToggle pattern: `role="switch"`, `aria-checked={enabled}`, `aria-label="Push Notifications"`.
    - Description text: "Send alerts to your phone via ntfy when farm conditions need attention." (14px/400 Inter muted, per UI-SPEC).
    - When disabled (enabled=false AND url is empty): show empty state "Push notifications are off. Enter your ntfy server URL and topic to enable." (per UI-SPEC).
    - Two text inputs: ntfy Server URL, Topic.
      - Input style: `background: var(--color-bg)`, `border: 1px solid var(--color-border)`, `border-radius: var(--radius-sm)`, `padding: var(--spacing-sm) var(--spacing-md)`, `color: var(--color-text-primary)`, `font-family: Inter`, `font-size: 16px`.
      - Focus: `border-color: var(--color-accent)`, `outline: none`.
      - Each input has `<label>` with matching `for`/`id` (accessibility).
    - "Send Test" button: primary style (`background: var(--color-accent)`, `color: #0f1117`, `font-weight: 600`, `border-radius: var(--radius-md)`, `min-height: 44px`, `padding: 0 var(--spacing-lg)`). Loading: text "Sending...", `aria-busy="true"`.
    - "Save Settings" button: same primary style as Send Test.
    - Settings save is NOT optimistic (per UI-SPEC Interaction Contract) -- wait for response, then update. On failure: toast "Settings update failed -- check connection".
    - Send Test: POST, loading state, success toast "Test notification sent -- check your phone", failure toast "Test failed -- verify ntfy URL and topic" (exact copy from UI-SPEC).

    **NtfySettingsForm.test.ts:**

    - `test_renders_url_and_topic_inputs`: render with settings={url: '', topic: '', enabled: false}, assert inputs present.
    - `test_renders_send_test_button`: assert button with text "Send Test" exists.
    - `test_renders_save_button`: assert button with text "Save Settings" exists.
    - `test_renders_empty_state_when_disabled`: assert "Push notifications are off" text.

    **StoragePanel.svelte (per UI-SPEC StoragePanel component):**

    Create `hub/dashboard/src/lib/StoragePanel.svelte`:

    Props: `stats: StorageStats`, `onpurge: () => Promise<void>`.

    Internal state:
    - `let showConfirm = $state(false)`.
    - `let purgeLoading = $state(false)`.

    Layout (per UI-SPEC):
    - Table of per-table sizes: tablename column (16px/400), size column (28px/600 tabular-nums right-aligned).
    - Retention policy info rows: label 14px/400 muted, value 16px/400 primary.
      - "Raw readings older than 90 days are automatically purged. Hourly rollups are retained for 2 years." (exact copy from UI-SPEC).
    - "Purge Now" button: destructive style (`border: 1px solid var(--color-offline)`, `color: var(--color-offline)`, `background: transparent`, `min-height: 44px`).
    - On Purge Now click: set `showConfirm = true`, show inline confirmation panel:
      - Panel background: `rgba(239, 68, 68, 0.08)`.
      - Border: `1px solid var(--color-offline)`.
      - Warning text (role="alert"): "This will permanently delete all raw sensor readings older than 90 days. This cannot be undone." (14px/400 Inter, `color: var(--color-text-primary)`).
      - Two buttons: "Keep My Data" (ghost style, cancels) and "Confirm Purge" (filled destructive: `background: var(--color-offline)`, `color: #f1f5f9`, `aria-busy` during loading).
    - On Confirm Purge: call `onpurge()`, show toast "Purge complete -- storage reclaimed" on success, "Purge failed -- check logs" on failure.
    - On Keep My Data: set `showConfirm = false`.

    **StoragePanel.test.ts:**

    - `test_renders_table_sizes`: render with mock stats, assert table name and size text.
    - `test_renders_purge_button`: assert "Purge Now" button exists.
    - `test_shows_confirmation_on_purge_click`: click Purge Now, assert "This will permanently delete" text appears.
    - `test_hides_confirmation_on_keep_data`: click Purge Now, click "Keep My Data", assert confirmation gone.

    **Notifications settings page (/settings/notifications/+page.svelte):**

    Create `hub/dashboard/src/routes/settings/notifications/+page.svelte`:

    ```svelte
    <svelte:head>
      <title>Farm -- Push Notifications</title>
    </svelte:head>
    ```

    - On mount: `fetch('/api/settings/notifications')` to load current NtfySettings.
    - Render with settings-page/settings-heading pattern.
    - Page heading: "Push Notifications" (per UI-SPEC).
    - Render `<NtfySettingsForm>` with loaded settings.
    - `onsave`: PATCH `/api/settings/notifications` with updated settings. On success: toast "Notification settings saved".
    - `ontest`: POST `/api/settings/notifications/test`. Return true/false.

    **Storage settings page (/settings/storage/+page.svelte):**

    Create `hub/dashboard/src/routes/settings/storage/+page.svelte`:

    ```svelte
    <svelte:head>
      <title>Farm -- Storage & Retention</title>
    </svelte:head>
    ```

    - On mount: `fetch('/api/storage/stats')` to load StorageStats.
    - Render with settings-page/settings-heading pattern.
    - Page heading: "Storage & Retention" (per UI-SPEC).
    - Render `<StoragePanel>` with loaded stats.
    - `onpurge`: POST `/api/storage/purge`. On success: reload stats. On failure: handled by StoragePanel.
    - Empty state: "Storage data unavailable -- reconnect to refresh." (per UI-SPEC).

    **Settings navigation (hub/dashboard/src/routes/+layout.svelte):**

    Read the current +layout.svelte. In the header area where the settings icon links to /settings/ai, extend the navigation to include all four settings pages. Add a small dropdown or direct links:

    Look at the existing header structure. The settings icon (lucide-svelte Settings) currently links directly to /settings/ai. Change this to navigate to a settings index or add a dropdown with links to:
    - /settings/ai (AI Engine)
    - /settings/calibration (Calibration)
    - /settings/notifications (Notifications)
    - /settings/storage (Storage)

    Implementation approach: Replace the single settings icon link with a settings icon that toggles a dropdown menu. The dropdown shows the four settings page links. Use a simple `$state` boolean for open/close. Click outside closes it.

    Alternatively, if the layout is simple enough, add a small settings sub-navigation bar at the top of all /settings/* pages. The simplest approach: keep the settings icon linking to /settings/ai, but add a sub-navigation within each settings page that shows links to all four settings routes. This can be done with a shared settings layout.

    Preferred: Create `hub/dashboard/src/routes/settings/+layout.svelte` (shared settings layout) that renders a horizontal tab bar with links to all four settings sub-pages. Each settings page inherits this layout automatically via SvelteKit's nested layout system.

    The settings sub-nav tabs:
    - "AI" -> /settings/ai
    - "Calibration" -> /settings/calibration
    - "Notifications" -> /settings/notifications
    - "Storage" -> /settings/storage

    Style: same tab pattern as the zone detail range-toggle (14px, muted color, accent underline for active). Use `$page.url.pathname` to determine active tab.
  </action>
  <verify>
    <automated>cd /Users/patrick.lewis/code/backyard-farm/hub/dashboard && npx vitest run src/lib/NtfySettingsForm.test.ts src/lib/StoragePanel.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - hub/dashboard/src/lib/NtfySettingsForm.svelte contains "Send Test"
    - hub/dashboard/src/lib/NtfySettingsForm.svelte contains "Save Settings"
    - hub/dashboard/src/lib/NtfySettingsForm.svelte contains "Push notifications are off"
    - hub/dashboard/src/lib/NtfySettingsForm.svelte contains 'role="switch"'
    - hub/dashboard/src/lib/NtfySettingsForm.svelte contains "Sending..."
    - hub/dashboard/src/lib/StoragePanel.svelte contains "Purge Now"
    - hub/dashboard/src/lib/StoragePanel.svelte contains "Keep My Data"
    - hub/dashboard/src/lib/StoragePanel.svelte contains "Confirm Purge"
    - hub/dashboard/src/lib/StoragePanel.svelte contains "This will permanently delete"
    - hub/dashboard/src/lib/StoragePanel.svelte contains "rgba(239, 68, 68, 0.08)"
    - hub/dashboard/src/routes/settings/notifications/+page.svelte contains "Push Notifications"
    - hub/dashboard/src/routes/settings/notifications/+page.svelte contains "api/settings/notifications"
    - hub/dashboard/src/routes/settings/storage/+page.svelte contains "Storage & Retention"
    - hub/dashboard/src/routes/settings/storage/+page.svelte contains "api/storage"
    - Settings sub-navigation exists with links to all four settings pages
    - NtfySettingsForm.test.ts tests pass
    - StoragePanel.test.ts tests pass
    - npm run build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>ntfy settings page configurable at runtime with toggle, URL, topic, Send Test button. Storage page shows per-table sizes with Purge Now + inline confirmation. Settings navigation connects all four settings pages. All component tests pass. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> fetch calls | Form inputs (ntfy URL, calibration values) sent to API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | NtfySettingsForm | mitigate | Pydantic validates at API boundary (Plan 02 T-05-04); frontend does basic URL format check |
| T-05-10 | Information Disclosure | StoragePanel | accept | Storage sizes are not sensitive; LAN-only access; farmer's own data |
| T-05-11 | Denial of Service | Purge Now button | mitigate | Inline confirmation dialog prevents accidental purge; confirmation text explicitly warns about data loss |
</threat_model>

<verification>
- All dashboard component tests pass: `cd hub/dashboard && npx vitest run`
- Build succeeds: `cd hub/dashboard && npm run build`
- CalibrationStatusBadge renders correct states for null, overdue, due soon, current
- NtfySettingsForm renders all required inputs and buttons per UI-SPEC
- StoragePanel shows confirmation dialog before purge
- Settings navigation links to all four settings pages
</verification>

<success_criteria>
- Calibration management page shows per-sensor status with overdue badges (D-05)
- Zone detail has inline "Record Calibration" action for pH (D-02)
- ntfy settings page has URL, topic, toggle, Send Test (D-07)
- Storage page shows per-table sizes and Purge Now with confirmation (D-12)
- All copy matches UI-SPEC copywriting contract exactly
- All component tests pass
- Build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-hardening/05-03-SUMMARY.md`
</output>
