---
phase: 02-actuator-control-alerts-and-dashboard-v1
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - hub/dashboard/src/routes/+layout.svelte
  - hub/dashboard/src/lib/TabBar.svelte
  - hub/dashboard/src/lib/AlertBar.svelte
  - hub/dashboard/src/lib/Toast.svelte
  - hub/dashboard/src/lib/CommandButton.svelte
  - hub/dashboard/src/lib/HealthBadge.svelte
  - hub/dashboard/src/lib/ws.svelte.ts
  - hub/dashboard/src/lib/ZoneCard.svelte
  - hub/dashboard/src/routes/+page.svelte
  - hub/dashboard/src/routes/coop/+page.svelte
  - hub/dashboard/src/routes/zones/[id]/+page.svelte
  - hub/dashboard/src/routes/recommendations/+page.svelte
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-06

must_haves:
  truths:
    - "Bottom tab bar with 3 tabs (Zones/Coop/Recs) is visible on all routes"
    - "Active tab is highlighted with accent color underline"
    - "Alert bar renders P0 alerts in red, P1 in amber, grouped with count"
    - "Alert bar rows are tappable and deep-link to the relevant route"
    - "Toast appears fixed above tab bar and auto-dismisses after 5 seconds"
    - "WebSocket store handles all Phase 2 delta types"
    - "ZoneCard is tappable and navigates to /zones/[id]"
    - "HealthBadge shows GOOD/WARN/CRIT with correct colors"
  artifacts:
    - path: "hub/dashboard/src/lib/TabBar.svelte"
      provides: "Bottom navigation component"
      contains: "aria-label=\"Main navigation\""
    - path: "hub/dashboard/src/lib/AlertBar.svelte"
      provides: "Persistent alert bar"
      contains: "role=\"alert\""
    - path: "hub/dashboard/src/lib/Toast.svelte"
      provides: "Error toast component"
      contains: "auto-dismiss"
    - path: "hub/dashboard/src/lib/CommandButton.svelte"
      provides: "Spinner + disabled command button wrapper"
      contains: "aria-disabled"
    - path: "hub/dashboard/src/lib/HealthBadge.svelte"
      provides: "Zone health badge"
      contains: "aria-label"
    - path: "hub/dashboard/src/lib/ws.svelte.ts"
      provides: "Extended WebSocket store with Phase 2 handlers"
      contains: "alert_state"
  key_links:
    - from: "hub/dashboard/src/lib/AlertBar.svelte"
      to: "SvelteKit router"
      via: "goto() on alert row tap"
      pattern: "goto\\("
    - from: "hub/dashboard/src/lib/ws.svelte.ts"
      to: "hub/dashboard/src/lib/types.ts"
      via: "Imports Phase 2 delta types"
      pattern: "AlertStateDelta|RecommendationQueueDelta"
---

<objective>
Frontend foundation — routing, layout, shared components, and WebSocket store extension.

Purpose: Transform the single-page dashboard into a multi-route SPA with bottom tab bar navigation, persistent alert bar, and all shared UI components that feature pages will use. Extend the WebSocket store to handle all Phase 2 delta types. This plan delivers the app shell that plans 02-05 and 02-06 build upon.

Output: Working multi-route SPA with TabBar, AlertBar, Toast, CommandButton, HealthBadge components. Route stubs for /coop, /zones/[id], /recommendations. WebSocket store extended with Phase 2 state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-actuator-control-alerts-and-dashboard-v1/02-CONTEXT.md
@.planning/phases/02-actuator-control-alerts-and-dashboard-v1/02-UI-SPEC.md
@.planning/phases/02-actuator-control-alerts-and-dashboard-v1/02-01-SUMMARY.md

<interfaces>
From hub/dashboard/src/lib/types.ts (extended in plan 02-01):
```typescript
export interface AlertEntry {
  key: string;
  severity: AlertSeverity;
  message: string;
  deep_link: string;
  count: number;
}

export interface Recommendation {
  recommendation_id: string;
  zone_id: string;
  rec_type: string;
  action_description: string;
  sensor_reading: string;
  explanation: string;
}

export type HealthScore = 'green' | 'yellow' | 'red';
export type DoorState = 'open' | 'closed' | 'moving' | 'stuck';
export type IrrigationState = 'closed' | 'open' | 'moving';

export interface DashboardSnapshot {
  type: 'snapshot';
  zones: Record<string, Record<string, SensorReading>>;
  nodes: Record<string, NodeState>;
  alerts: AlertEntry[];
  recommendations: Recommendation[];
  actuator_states: Record<string, string>;
  zone_health_scores: Record<string, { score: HealthScore; contributing_sensors: string[] }>;
  feed_level: { percentage: number; below_threshold: boolean } | null;
  water_level: { percentage: number; below_threshold: boolean } | null;
  coop_schedule: CoopSchedule | null;
}
```

From hub/dashboard/src/lib/ws.svelte.ts (existing):
```typescript
class DashboardStore {
  zones = $state<Map<string, ZoneState>>(new Map());
  nodes = $state<Map<string, NodeState>>(new Map());
  connectionStatus = $state<ConnectionStatus>('disconnected');
  // ... connect(), handleMessage(), etc.
}
export const dashboardStore = new DashboardStore();
```

From hub/dashboard/src/app.css:
```css
:root {
  --color-bg: #0f1117;
  --color-surface: #1c1f2b;
  --color-accent: #4ade80;
  --color-stale: #f59e0b;
  --color-stuck: #f97316;
  --color-offline: #ef4444;
  --color-muted: #6b7280;
  --color-border: #2d3149;
  --color-text-primary: #f1f5f9;
  --color-text-secondary: #94a3b8;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WebSocket store extension + shared components (TabBar, AlertBar, Toast, CommandButton, HealthBadge)</name>
  <files>
    hub/dashboard/src/lib/ws.svelte.ts,
    hub/dashboard/src/lib/TabBar.svelte,
    hub/dashboard/src/lib/AlertBar.svelte,
    hub/dashboard/src/lib/Toast.svelte,
    hub/dashboard/src/lib/CommandButton.svelte,
    hub/dashboard/src/lib/HealthBadge.svelte
  </files>
  <read_first>
    hub/dashboard/src/lib/ws.svelte.ts,
    hub/dashboard/src/lib/types.ts,
    hub/dashboard/src/app.css,
    hub/dashboard/src/lib/ZoneCard.svelte,
    hub/dashboard/src/lib/SensorValue.svelte,
    .planning/phases/02-actuator-control-alerts-and-dashboard-v1/02-UI-SPEC.md
  </read_first>
  <action>
**hub/dashboard/src/lib/ws.svelte.ts** — extend DashboardStore class:

Add new state properties after existing `connectionStatus`:
```typescript
alerts = $state<AlertEntry[]>([]);
recommendations = $state<Recommendation[]>([]);
actuatorStates = $state<Map<string, string>>(new Map());
zoneHealthScores = $state<Map<string, { score: HealthScore; contributing_sensors: string[] }>>(new Map());
feedLevel = $state<{ percentage: number; below_threshold: boolean } | null>(null);
waterLevel = $state<{ percentage: number; below_threshold: boolean } | null>(null);
coopSchedule = $state<CoopSchedule | null>(null);
```

Add imports for new types:
```typescript
import type { ZoneState, NodeState, SensorReading, ConnectionStatus, WSMessage, AlertEntry, Recommendation, HealthScore, CoopSchedule } from './types';
```

Extend `handleMessage()` — in the snapshot handler, after existing zone/node processing, add:
```typescript
this.alerts = msg.alerts ?? [];
this.recommendations = msg.recommendations ?? [];
const newActuators = new Map<string, string>();
for (const [key, state] of Object.entries(msg.actuator_states ?? {})) {
  newActuators.set(key, state);
}
this.actuatorStates = newActuators;
const newScores = new Map<string, { score: HealthScore; contributing_sensors: string[] }>();
for (const [zoneId, data] of Object.entries(msg.zone_health_scores ?? {})) {
  newScores.set(zoneId, data);
}
this.zoneHealthScores = newScores;
this.feedLevel = msg.feed_level ?? null;
this.waterLevel = msg.water_level ?? null;
this.coopSchedule = msg.coop_schedule ?? null;
```

Add delta handlers after existing heartbeat handler:
```typescript
} else if (msg.type === 'alert_state') {
  this.alerts = msg.alerts;
} else if (msg.type === 'recommendation_queue') {
  this.recommendations = msg.recommendations;
} else if (msg.type === 'actuator_state') {
  const key = msg.zone_id ? `${msg.device}:${msg.zone_id}` : msg.device;
  this.actuatorStates = new Map(this.actuatorStates.set(key, msg.state));
} else if (msg.type === 'zone_health_score') {
  this.zoneHealthScores = new Map(this.zoneHealthScores.set(msg.zone_id, {
    score: msg.score,
    contributing_sensors: msg.contributing_sensors,
  }));
} else if (msg.type === 'feed_level') {
  this.feedLevel = { percentage: msg.percentage, below_threshold: msg.below_threshold };
} else if (msg.type === 'water_level') {
  this.waterLevel = { percentage: msg.percentage, below_threshold: msg.below_threshold };
} else if (msg.type === 'coop_schedule') {
  this.coopSchedule = msg.schedule;
}
```

**hub/dashboard/src/lib/TabBar.svelte** — create per UI-SPEC:

```svelte
<script lang="ts">
  import { page } from '$app/stores';
  import { Sprout, Home, Bell } from 'lucide-svelte';

  const tabs = [
    { path: '/', label: 'Zones', icon: Sprout, ariaLabel: 'Zones' },
    { path: '/coop', label: 'Coop', icon: Home, ariaLabel: 'Coop' },
    { path: '/recommendations', label: 'Recs', icon: Bell, ariaLabel: 'Recommendations' },
  ] as const;

  function isActive(tabPath: string, currentPath: string): boolean {
    if (tabPath === '/') return currentPath === '/' || currentPath.startsWith('/zones');
    return currentPath.startsWith(tabPath);
  }
</script>

<nav aria-label="Main navigation" class="tab-bar">
  {#each tabs as tab}
    <a
      href={tab.path}
      class="tab"
      class:active={isActive(tab.path, $page.url.pathname)}
      aria-current={isActive(tab.path, $page.url.pathname) ? 'page' : undefined}
      aria-label={tab.ariaLabel}
    >
      <tab.icon size={20} />
      <span class="tab-label">{tab.label}</span>
    </a>
  {/each}
</nav>

<style>
  .tab-bar {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    height: calc(56px + env(safe-area-inset-bottom, 0px));
    padding-bottom: env(safe-area-inset-bottom, 0px);
    background: var(--color-surface);
    border-top: 1px solid var(--color-border);
    display: flex;
    z-index: 20;
  }
  .tab {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-xs);
    min-height: 44px;
    text-decoration: none;
    color: var(--color-muted);
    font-size: 14px;
    font-weight: 400;
    line-height: 1.4;
    border-bottom: 2px solid transparent;
    transition: color 0.15s;
  }
  .tab.active {
    color: var(--color-accent);
    border-bottom-color: var(--color-accent);
  }
  .tab-label {
    font-size: 14px;
  }
</style>
```

**hub/dashboard/src/lib/AlertBar.svelte** — create per UI-SPEC:

```svelte
<script lang="ts">
  import { goto } from '$app/navigation';
  import { AlertTriangle } from 'lucide-svelte';
  import type { AlertEntry } from './types';

  let { alerts = [] }: { alerts: AlertEntry[] } = $props();
</script>

{#if alerts.length > 0}
<div class="alert-bar" role="alert">
  {#each alerts as alert}
    <button
      class="alert-row"
      class:p0={alert.severity === 'P0'}
      class:p1={alert.severity === 'P1'}
      aria-live={alert.severity === 'P0' ? 'assertive' : 'polite'}
      onclick={() => goto(alert.deep_link)}
      role="button"
      tabindex="0"
    >
      <span class="severity-bar"></span>
      <AlertTriangle size={16} />
      <span class="alert-text">{alert.message}</span>
      {#if alert.count > 1}
        <span class="count-badge">{alert.count}</span>
      {/if}
    </button>
  {/each}
</div>
{/if}

<style>
  .alert-bar {
    width: 100%;
    background: var(--color-surface);
    border-bottom: 1px solid var(--color-border);
  }
  .alert-row {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    padding: var(--spacing-sm) var(--spacing-md);
    min-height: 40px;
    width: 100%;
    border: none;
    background: none;
    cursor: pointer;
    text-align: left;
    font-family: inherit;
    font-size: 16px;
    font-weight: 400;
    line-height: 1.5;
    color: var(--color-text-primary);
  }
  .alert-row:not(:last-child) {
    border-bottom: 1px solid var(--color-border);
  }
  .severity-bar {
    width: 4px;
    align-self: stretch;
    border-radius: 2px;
    flex-shrink: 0;
  }
  .p0 .severity-bar { background: var(--color-offline); }
  .p0 :global(svg) { color: var(--color-offline); }
  .p1 .severity-bar { background: var(--color-stale); }
  .p1 :global(svg) { color: var(--color-stale); }
  .alert-text {
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .count-badge {
    font-size: 14px;
    font-weight: 400;
    line-height: 1.4;
    padding: 2px var(--spacing-sm);
    background: var(--color-border);
    color: var(--color-text-primary);
    border-radius: 99px;
    flex-shrink: 0;
  }
</style>
```

**hub/dashboard/src/lib/Toast.svelte** — create per UI-SPEC:

```svelte
<script lang="ts">
  import { XCircle } from 'lucide-svelte';

  let { message = '', visible = $bindable(false) }: { message: string; visible: boolean } = $props();

  let timer: ReturnType<typeof setTimeout> | null = null;

  $effect(() => {
    if (visible) {
      if (timer) clearTimeout(timer);
      timer = setTimeout(() => { visible = false; }, 5000);
    }
  });
</script>

{#if visible}
<div class="toast" role="alert" aria-live="assertive">
  <XCircle size={16} />
  <span>{message}</span>
</div>
{/if}

<style>
  .toast {
    position: fixed;
    bottom: calc(56px + env(safe-area-inset-bottom, 0px) + 8px);
    left: 50%;
    transform: translateX(-50%);
    width: min(calc(100% - 32px), 400px);
    background: var(--color-offline);
    color: var(--color-text-primary);
    border-radius: 8px;
    padding: var(--spacing-sm);
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    font-size: 16px;
    font-weight: 400;
    line-height: 1.5;
    z-index: 30;
  }
</style>
```

**hub/dashboard/src/lib/CommandButton.svelte** — create per UI-SPEC:

```svelte
<script lang="ts">
  import { Loader2 } from 'lucide-svelte';

  let {
    label = '',
    variant = 'default',
    loading = false,
    disabled = false,
    onclick = () => {},
  }: {
    label: string;
    variant?: 'default' | 'approve' | 'reject';
    loading?: boolean;
    disabled?: boolean;
    onclick?: () => void;
  } = $props();
</script>

<button
  class="cmd-btn"
  class:approve={variant === 'approve'}
  class:reject={variant === 'reject'}
  class:loading
  disabled={loading || disabled}
  aria-disabled={loading || disabled}
  aria-busy={loading}
  onclick={onclick}
>
  {#if loading}
    <span class="spinner"><Loader2 size={16} /></span>
  {:else}
    {label}
  {/if}
</button>

<style>
  .cmd-btn {
    min-height: 44px;
    padding: var(--spacing-sm) var(--spacing-md);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    background: var(--color-surface);
    color: var(--color-text-primary);
    font-family: inherit;
    font-size: 16px;
    font-weight: 600;
    line-height: 1.5;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-sm);
    transition: opacity 0.15s;
  }
  .cmd-btn:disabled {
    cursor: not-allowed;
    opacity: 0.6;
  }
  .cmd-btn.approve {
    background: var(--color-accent);
    color: var(--color-bg);
    border-color: var(--color-accent);
  }
  .cmd-btn.reject {
    background: var(--color-surface);
    border-color: var(--color-border);
    color: var(--color-text-primary);
  }
  .spinner {
    display: inline-flex;
    animation: spin 1s linear infinite;
    color: var(--color-accent);
  }
  .cmd-btn.approve .spinner {
    color: var(--color-bg);
  }
  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
</style>
```

**hub/dashboard/src/lib/HealthBadge.svelte** — create per UI-SPEC:

```svelte
<script lang="ts">
  import type { HealthScore } from './types';

  let { score = 'green' as HealthScore }: { score: HealthScore } = $props();

  const LABELS: Record<HealthScore, string> = {
    green: 'GOOD',
    yellow: 'WARN',
    red: 'CRIT',
  };

  const ARIA_LABELS: Record<HealthScore, string> = {
    green: 'Zone health: Good',
    yellow: 'Zone health: Warning',
    red: 'Zone health: Critical',
  };
</script>

<span
  class="health-badge"
  class:green={score === 'green'}
  class:yellow={score === 'yellow'}
  class:red={score === 'red'}
  aria-label={ARIA_LABELS[score]}
>
  {LABELS[score]}
</span>

<style>
  .health-badge {
    display: inline-block;
    font-size: 14px;
    font-weight: 400;
    line-height: 1.4;
    padding: 2px var(--spacing-xs);
    border-radius: 99px;
  }
  .green {
    background: var(--color-accent);
    color: var(--color-bg);
  }
  .yellow {
    background: var(--color-stale);
    color: var(--color-bg);
  }
  .red {
    background: var(--color-offline);
    color: var(--color-text-primary);
  }
</style>
```
  </action>
  <verify>
    <automated>cd /Users/patrick.lewis/code/backyard-farm/hub/dashboard && npx svelte-kit sync 2>&1 | tail -5 && grep -q "alert_state" src/lib/ws.svelte.ts && grep -q "aria-label=\"Main navigation\"" src/lib/TabBar.svelte && grep -q "role=\"alert\"" src/lib/AlertBar.svelte && grep -q "aria-disabled" src/lib/CommandButton.svelte && grep -q "aria-label" src/lib/HealthBadge.svelte && echo "Components OK"</automated>
  </verify>
  <acceptance_criteria>
    - hub/dashboard/src/lib/ws.svelte.ts contains `alerts = $state<AlertEntry[]>([])`
    - hub/dashboard/src/lib/ws.svelte.ts contains `recommendations = $state<Recommendation[]>([])`
    - hub/dashboard/src/lib/ws.svelte.ts contains `actuatorStates = $state<Map<string, string>>`
    - hub/dashboard/src/lib/ws.svelte.ts contains `zoneHealthScores = $state<Map<`
    - hub/dashboard/src/lib/ws.svelte.ts contains `msg.type === 'alert_state'`
    - hub/dashboard/src/lib/ws.svelte.ts contains `msg.type === 'recommendation_queue'`
    - hub/dashboard/src/lib/ws.svelte.ts contains `msg.type === 'actuator_state'`
    - hub/dashboard/src/lib/TabBar.svelte contains `aria-label="Main navigation"`
    - hub/dashboard/src/lib/TabBar.svelte contains `aria-current`
    - hub/dashboard/src/lib/AlertBar.svelte contains `role="alert"`
    - hub/dashboard/src/lib/AlertBar.svelte contains `goto(alert.deep_link)`
    - hub/dashboard/src/lib/Toast.svelte contains `setTimeout(() => { visible = false; }, 5000)`
    - hub/dashboard/src/lib/CommandButton.svelte contains `aria-disabled`
    - hub/dashboard/src/lib/CommandButton.svelte contains `aria-busy`
    - hub/dashboard/src/lib/HealthBadge.svelte contains `GOOD`
    - hub/dashboard/src/lib/HealthBadge.svelte contains `WARN`
    - hub/dashboard/src/lib/HealthBadge.svelte contains `CRIT`
  </acceptance_criteria>
  <done>All shared Phase 2 components created. WebSocket store extended to handle all Phase 2 delta types.</done>
</task>

<task type="auto">
  <name>Task 2: Layout rewrite + route scaffolds (zones, coop, recommendations) + ZoneCard extension</name>
  <files>
    hub/dashboard/src/routes/+layout.svelte,
    hub/dashboard/src/routes/+page.svelte,
    hub/dashboard/src/routes/coop/+page.svelte,
    hub/dashboard/src/routes/zones/[id]/+page.svelte,
    hub/dashboard/src/routes/recommendations/+page.svelte,
    hub/dashboard/src/lib/ZoneCard.svelte
  </files>
  <read_first>
    hub/dashboard/src/routes/+layout.svelte,
    hub/dashboard/src/routes/+page.svelte,
    hub/dashboard/src/lib/ZoneCard.svelte,
    hub/dashboard/src/lib/ws.svelte.ts,
    hub/dashboard/src/lib/types.ts,
    hub/dashboard/src/app.css,
    .planning/phases/02-actuator-control-alerts-and-dashboard-v1/02-UI-SPEC.md
  </read_first>
  <action>
**hub/dashboard/src/routes/+layout.svelte** — rewrite to include TabBar, AlertBar, WebSocket lifecycle, and Toast:

```svelte
<script>
  import '../app.css';
  import { onMount, onDestroy } from 'svelte';
  import { dashboardStore } from '$lib/ws.svelte';
  import TabBar from '$lib/TabBar.svelte';
  import AlertBar from '$lib/AlertBar.svelte';
  import Toast from '$lib/Toast.svelte';

  let { children } = $props();

  let toastMessage = $state('');
  let toastVisible = $state(false);

  // Expose toast function globally via a simple event pattern
  // Components dispatch 'toast' CustomEvent on window
  function handleToast(event) {
    toastMessage = event.detail?.message ?? 'An error occurred';
    toastVisible = true;
  }

  onMount(() => {
    dashboardStore.connect();
    window.addEventListener('farm:toast', handleToast);
  });

  onDestroy(() => {
    dashboardStore.disconnect();
    window.removeEventListener('farm:toast', handleToast);
  });
</script>

<div class="app-layout">
  <header class="app-header">
    <h1 class="header-title">Farm Dashboard</h1>
    <span
      class="ws-dot"
      class:connected={dashboardStore.connectionStatus === 'connected'}
      class:reconnecting={dashboardStore.connectionStatus === 'reconnecting'}
      title={dashboardStore.connectionStatus}
    ></span>
  </header>

  <AlertBar alerts={dashboardStore.alerts} />

  <main class="page-content">
    {@render children()}
  </main>

  <TabBar />
  <Toast message={toastMessage} bind:visible={toastVisible} />
</div>

<style>
  .app-layout {
    display: flex;
    flex-direction: column;
    min-height: 100dvh;
  }
  .app-header {
    position: sticky;
    top: 0;
    height: 48px;
    background: var(--color-surface);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 var(--spacing-md);
    z-index: 10;
    border-bottom: 1px solid var(--color-border);
  }
  .header-title {
    font-size: 20px;
    font-weight: 600;
    line-height: 1.2;
    color: var(--color-text-primary);
  }
  .ws-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--color-offline);
    flex-shrink: 0;
  }
  .ws-dot.connected { background: var(--color-accent); }
  .ws-dot.reconnecting { background: var(--color-stale); }
  .page-content {
    flex: 1;
    padding: var(--spacing-md);
    padding-bottom: calc(56px + env(safe-area-inset-bottom, 0px) + var(--spacing-md));
  }
</style>
```

**hub/dashboard/src/lib/ZoneCard.svelte** — extend with tappable navigation and HealthBadge. Read the existing file first to preserve its current sensor display. Add:
- Import `goto` from `'$app/navigation'`
- Import `HealthBadge` from `'./HealthBadge.svelte'`
- Accept new prop: `healthScore` of type `HealthScore | undefined`
- Wrap the card in a clickable container: add `role="button"`, `tabindex="0"`, `onclick={() => goto(\`/zones/${zone.zone_id}\`)}`, `onkeydown` for Enter/Space
- Add `cursor: pointer` to card style
- Add HealthBadge to the card header row (right side, inline with zone name)

**hub/dashboard/src/routes/+page.svelte** — update to pass healthScore prop to ZoneCard. Read the existing file first. Add the health score lookup from `dashboardStore.zoneHealthScores.get(zone.zone_id)?.score` and pass as prop.

**hub/dashboard/src/routes/coop/+page.svelte** — create stub route:

```svelte
<script>
  import { dashboardStore } from '$lib/ws.svelte';
</script>

<h2 class="page-heading">Coop</h2>
<p class="placeholder">Coop panel will be implemented in plan 02-05.</p>

<style>
  .page-heading {
    font-size: 20px;
    font-weight: 600;
    line-height: 1.2;
    margin-bottom: var(--spacing-lg);
  }
  .placeholder {
    color: var(--color-muted);
    font-size: 16px;
  }
</style>
```

**hub/dashboard/src/routes/zones/[id]/+page.svelte** — create stub route:

```svelte
<script>
  import { page } from '$app/stores';
  import { dashboardStore } from '$lib/ws.svelte';

  const zoneId = $derived($page.params.id);
</script>

<h2 class="page-heading">{zoneId}</h2>
<p class="placeholder">Zone detail will be implemented in plan 02-05.</p>

<style>
  .page-heading {
    font-size: 20px;
    font-weight: 600;
    line-height: 1.2;
    margin-bottom: var(--spacing-lg);
  }
  .placeholder {
    color: var(--color-muted);
    font-size: 16px;
  }
</style>
```

**hub/dashboard/src/routes/recommendations/+page.svelte** — create stub route:

```svelte
<script>
  import { dashboardStore } from '$lib/ws.svelte';
</script>

<h2 class="page-heading">Recommendations</h2>
<p class="placeholder">Recommendation queue will be implemented in plan 02-05.</p>

<style>
  .page-heading {
    font-size: 20px;
    font-weight: 600;
    line-height: 1.2;
    margin-bottom: var(--spacing-lg);
  }
  .placeholder {
    color: var(--color-muted);
    font-size: 16px;
  }
</style>
```
  </action>
  <verify>
    <automated>cd /Users/patrick.lewis/code/backyard-farm/hub/dashboard && npx svelte-kit sync 2>&1 | tail -5 && ls src/routes/coop/+page.svelte src/routes/zones/\[id\]/+page.svelte src/routes/recommendations/+page.svelte 2>&1 && grep -q "TabBar" src/routes/+layout.svelte && grep -q "AlertBar" src/routes/+layout.svelte && grep -q "goto" src/lib/ZoneCard.svelte && echo "Routes OK"</automated>
  </verify>
  <acceptance_criteria>
    - hub/dashboard/src/routes/+layout.svelte contains `import TabBar from '$lib/TabBar.svelte'`
    - hub/dashboard/src/routes/+layout.svelte contains `import AlertBar from '$lib/AlertBar.svelte'`
    - hub/dashboard/src/routes/+layout.svelte contains `import Toast from '$lib/Toast.svelte'`
    - hub/dashboard/src/routes/+layout.svelte contains `dashboardStore.connect()`
    - hub/dashboard/src/routes/+layout.svelte contains `<TabBar />`
    - hub/dashboard/src/routes/+layout.svelte contains `<AlertBar alerts={dashboardStore.alerts}`
    - hub/dashboard/src/routes/coop/+page.svelte exists
    - hub/dashboard/src/routes/zones/[id]/+page.svelte exists
    - hub/dashboard/src/routes/recommendations/+page.svelte exists
    - hub/dashboard/src/lib/ZoneCard.svelte contains `goto(`
    - hub/dashboard/src/lib/ZoneCard.svelte contains `role="button"`
    - hub/dashboard/src/lib/ZoneCard.svelte contains `HealthBadge`
  </acceptance_criteria>
  <done>Layout rewritten with TabBar, AlertBar, Toast. All routes scaffolded. ZoneCard is tappable with HealthBadge. WebSocket connects in layout lifecycle. Navigation works between all tabs.</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation | Residual Risk |
|--------|----------|------------|---------------|
| XSS via alert message content | medium | Alert messages are constructed hub-side from zone_id strings (not user input); Svelte auto-escapes HTML in templates | If a zone_id contained script tags (unlikely — configured by farmer) |
| Deep-link navigation to invalid routes | low | SvelteKit 404 handler for unknown routes; goto() only uses hub-provided deep_link paths | Invalid zone_id in URL shows empty page |
</threat_model>

<verification>
- `cd hub/dashboard && npx svelte-kit sync` exits 0
- All 4 routes exist: `/`, `/coop`, `/zones/[id]`, `/recommendations`
- `npm run build` completes without errors (deferred to plan 02-06)
- WebSocket store has handlers for all 8 delta types
</verification>

<success_criteria>
- Multi-route SPA with bottom tab bar navigation working
- Alert bar renders from WebSocket state
- Toast auto-dismisses after 5 seconds
- ZoneCard navigates to zone detail on tap
- HealthBadge shows correct color/label per score
- All routes accessible via tab bar
</success_criteria>

<output>
After completion, create `.planning/phases/02-actuator-control-alerts-and-dashboard-v1/02-04-SUMMARY.md`
</output>
