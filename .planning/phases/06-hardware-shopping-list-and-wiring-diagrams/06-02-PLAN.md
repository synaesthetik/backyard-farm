---
phase: 06-hardware-shopping-list-and-wiring-diagrams
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hardware/README.md
  - docs/hardware/fritzing/README.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "The docs/hardware/ directory structure exists with all expected files scaffolded so subsystem doc writers (Plans 03-05) can reference exact paths"
    - "Fritzing workflow instructions exist for the author — how to install, create, export, and commit diagrams"
    - "Placeholder PNG files exist in docs/hardware/fritzing/ so Markdown references don't produce broken images before diagrams are drawn"
    - "The standard subsystem document template is documented so all 5 subsystem docs follow consistent structure"
  artifacts:
    - path: "docs/hardware/README.md"
      provides: "Navigation index for all hardware docs, Fritzing setup instructions, standard template"
      contains: "fritzing"
    - path: "docs/hardware/fritzing/README.md"
      provides: "Fritzing workflow and placeholder diagram tracking"
      contains: "fzz"
  key_links:
    - from: "docs/hardware/README.md"
      to: "docs/hardware/hub.md, docs/hardware/garden-node.md, docs/hardware/coop-node.md, docs/hardware/irrigation.md, docs/hardware/power.md, docs/hardware/bom.md"
      via: "Markdown links in index"
      pattern: "\\[.*\\]\\(.*\\.md\\)"
---

<objective>
Scaffold the complete docs/hardware/ directory structure, write the README navigation index, document the Fritzing workflow, create placeholder PNG files, and publish the standard subsystem document template.

Purpose: Plans 03, 04, and 05 write the five subsystem documents — they depend on the directory structure and template being established. This plan runs in Wave 1 alongside Plan 01 (BOM) so the two parallel tracks are unblocked.

Output: `docs/hardware/README.md` (navigation index + Fritzing workflow + standard template), `docs/hardware/fritzing/README.md` (workflow + placeholder tracking), and placeholder PNG files in `docs/hardware/fritzing/` for all four diagrams.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-hardware-shopping-list-and-wiring-diagrams/06-CONTEXT.md
@.planning/phases/06-hardware-shopping-list-and-wiring-diagrams/06-RESEARCH.md

<interfaces>
<!-- Directory structure locked by D-08 and D-09 from CONTEXT.md -->

Target directory layout (per D-09):
```
docs/
└── hardware/
    ├── README.md                  ← this plan creates
    ├── bom.md                     ← plan 01 creates
    ├── hub.md                     ← plan 03 creates
    ├── garden-node.md             ← plan 04 creates
    ├── coop-node.md               ← plan 05 creates
    ├── irrigation.md              ← plan 04 creates
    ├── power.md                   ← plan 03 creates
    └── fritzing/
        ├── README.md              ← this plan creates
        ├── garden-node.fzz        ← author creates with Fritzing (post-Phase 6)
        ├── garden-node.png        ← this plan creates placeholder
        ├── coop-node.fzz          ← author creates with Fritzing
        ├── coop-node.png          ← this plan creates placeholder
        ├── irrigation-relay.fzz   ← author creates with Fritzing
        ├── irrigation-relay.png   ← this plan creates placeholder
        └── power-distribution.fzz ← author creates with Fritzing
        └── power-distribution.png ← this plan creates placeholder
```

Standard subsystem document template (from RESEARCH.md, D-09):
1. # [Subsystem Name]
2. ## Parts Needed (links to bom.md anchors)
3. ## Overview Diagram (![alt](fritzing/name.png), Source: .fzz link)
4. ## Wiring Table (From | To | Wire Color | Notes columns)
5. ## GPIO Pin Assignment Table (BCM | Physical Pin | Function | Connected To)
6. ## Config File Cross-Reference (Hardware Connection | Config Variable | File | Value)
7. ## Smoke Test (numbered steps)
8. ## Common Mistakes (3-5 bullets)

Wire color convention (from RESEARCH.md):
Red = 3.3V/5V positive power
Orange = 12V DC
Black = Ground
Blue = I2C SDA
Yellow = I2C SCL
Green = GPIO signal / relay control
White = 1-Wire data
Gray = Relay COM
Purple = Relay NC terminal

Fritzing workflow (from RESEARCH.md):
- Version: Fritzing 1.0.6 (December 2025 latest stable)
- Download: fritzing.org (€8 minimum contribution for binaries, GPL source free to build)
- GitHub: github.com/fritzing/fritzing-app
- Parts repo for Pi Zero 2W: github.com/fritzing/fritzing-parts
- Create breadboard view (NOT schematic view — breadboard is what non-engineers read)
- Export: File → Export → As Image → PNG, 150 DPI minimum
- Save .fzz to docs/hardware/fritzing/[name].fzz
- Save PNG to docs/hardware/fritzing/[name].png
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold docs/hardware/ directory structure and write README.md</name>
  <files>docs/hardware/README.md</files>
  <read_first>
    - .planning/phases/06-hardware-shopping-list-and-wiring-diagrams/06-CONTEXT.md (D-05 through D-09: diagram format, directory structure)
    - .planning/phases/06-hardware-shopping-list-and-wiring-diagrams/06-RESEARCH.md (standard template, wire colors, Fritzing workflow)
  </read_first>
  <action>
Create `docs/hardware/README.md` with the following structure:

```markdown
# Backyard Farm Platform — Hardware Documentation

This directory contains everything needed to build the complete backyard farm platform
from scratch. Follow these documents in order:

## Build Order

1. **[Master BOM](bom.md)** — Buy everything first. Read this before ordering.
2. **[Power](power.md)** — Set up power distribution before connecting anything.
3. **[Hub](hub.md)** — Assemble and configure the hub Raspberry Pi.
4. **[Garden Node](garden-node.md)** — Build one garden zone node (template for all 4 zones).
5. **[Coop Node](coop-node.md)** — Assemble the coop edge node with actuator and sensors.
6. **[Irrigation](irrigation.md)** — Wire solenoid valves and relay board.

## Directory Contents

| File | Purpose |
|------|---------|
| [bom.md](bom.md) | Master bill of materials — all components, prices, purchase links |
| [hub.md](hub.md) | Hub Raspberry Pi 5 assembly, SSD setup, Docker Compose |
| [garden-node.md](garden-node.md) | Garden zone edge node — soil moisture, pH, temperature sensors |
| [coop-node.md](coop-node.md) | Coop edge node — linear actuator, limit switches, load cell |
| [irrigation.md](irrigation.md) | Solenoid valve wiring and relay board |
| [power.md](power.md) | 12V power distribution, buck converters, weatherproofing |
| [fritzing/](fritzing/) | Fritzing `.fzz` source files and PNG diagram exports |

## Standard Document Structure

Every subsystem document in this directory follows the same pattern:

1. **Parts Needed** — Which BOM components this subsystem uses (with links)
2. **Overview Diagram** — Fritzing breadboard-style wiring diagram
3. **Wiring Table** — Every connection listed: From | To | Wire Color | Notes
4. **GPIO Pin Assignment Table** — BCM number, physical pin, function, connected-to
5. **Config File Cross-Reference** — Maps each hardware connection to its config constant in the codebase
6. **Smoke Test** — Step-by-step power-on verification before connecting the next subsystem
7. **Common Mistakes** — The 3-5 most common wiring errors for this subsystem

## Wire Color Convention

All diagrams in this documentation use the following wire color conventions:

| Color | Function |
|-------|---------|
| Red | DC positive power (3.3V or 5V logic power rails) |
| Orange | 12V DC power |
| Black | Ground / DC negative |
| Blue | I2C SDA (data) |
| Yellow | I2C SCL (clock) |
| Green | GPIO signal or relay control |
| White | 1-Wire data (DS18B20 temperature sensors) |
| Gray | Relay COM (common) terminal |
| Purple | Relay NC (normally-closed) terminal |

## Creating and Updating Fritzing Diagrams

All wiring diagrams in this documentation were created with [Fritzing](https://fritzing.org) 1.0.6
(December 2025 latest stable release). The `.fzz` source files are committed under `docs/hardware/fritzing/`
so anyone can open and edit them.

### Installing Fritzing

1. Download from [fritzing.org](https://fritzing.org) — requires a minimum €8 contribution for pre-built binaries
2. Alternatively, build from source (GPL v3): [github.com/fritzing/fritzing-app](https://github.com/fritzing/fritzing-app)
3. If the Raspberry Pi Zero 2W part is missing from the default library, download it from:
   [github.com/fritzing/fritzing-parts](https://github.com/fritzing/fritzing-parts)

### Workflow for Creating/Updating a Diagram

1. Open the `.fzz` source file in Fritzing: `docs/hardware/fritzing/[name].fzz`
2. Switch to **Breadboard view** (not Schematic or PCB view — breadboard view is what readers expect)
3. Make your changes
4. Save the `.fzz` file: **File → Save**
5. Export as PNG: **File → Export → As Image → PNG**
   - Set resolution to 150 DPI minimum
   - Save to `docs/hardware/fritzing/[name].png`
6. Commit both the `.fzz` and `.png` files to git

> **Important:** Always commit the `.fzz` source file alongside the PNG. If only the PNG is committed,
> future contributors cannot edit the diagram when hardware changes.

### Current Diagram Status

| Diagram | .fzz Source | PNG Exported | Subsystem Doc |
|---------|------------|--------------|---------------|
| garden-node | [garden-node.fzz](fritzing/garden-node.fzz) | [garden-node.png](fritzing/garden-node.png) | [garden-node.md](garden-node.md) |
| coop-node | [coop-node.fzz](fritzing/coop-node.fzz) | [coop-node.png](fritzing/coop-node.png) | [coop-node.md](coop-node.md) |
| irrigation-relay | [irrigation-relay.fzz](fritzing/irrigation-relay.fzz) | [irrigation-relay.png](fritzing/irrigation-relay.png) | [irrigation.md](irrigation.md) |
| power-distribution | [power-distribution.fzz](fritzing/power-distribution.fzz) | [power-distribution.png](fritzing/power-distribution.png) | [power.md](power.md) |

> **Note for builders:** The Fritzing diagrams require the author to create them using the Fritzing
> application. The `.fzz` placeholder files in the `fritzing/` directory are tracked in git as
> future diagram locations. Until the author creates the actual diagrams, the wiring tables in
> each subsystem document provide the complete connection information. The diagrams are visual
> supplements to the wiring tables, not replacements for them.
```
  </action>
  <verify>
    <automated>grep -c "bom.md\|hub.md\|garden-node.md\|coop-node.md\|irrigation.md\|power.md\|fritzing\|Wire Color\|Smoke Test\|fritzing.org" docs/hardware/README.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - docs/hardware/README.md exists
    - Contains build order section with all 6 documents in correct sequence
    - Contains directory contents table linking all 6 md files and fritzing/
    - Contains standard document structure section listing all 8 sections
    - Contains wire color convention table (9 colors minimum)
    - Contains Fritzing workflow section with install instructions and export steps
    - Contains diagram status table tracking 4 diagrams (.fzz + .png status)
    - grep "fritzing.org" docs/hardware/README.md returns match
    - grep "github.com/fritzing" docs/hardware/README.md returns match
  </acceptance_criteria>
  <done>docs/hardware/README.md is a complete navigation index and reference for the hardware documentation directory. A new contributor can understand the full structure and workflow from this file alone.</done>
</task>

<task type="auto">
  <name>Task 2: Create fritzing/ subdirectory with README and placeholder files</name>
  <files>docs/hardware/fritzing/README.md</files>
  <read_first>
    - .planning/phases/06-hardware-shopping-list-and-wiring-diagrams/06-CONTEXT.md (D-06: .fzz files in docs/hardware/fritzing/, D-07: PNGs inline in docs)
    - .planning/phases/06-hardware-shopping-list-and-wiring-diagrams/06-RESEARCH.md (Pitfall 7: .fzz source must be committed)
  </read_first>
  <action>
Create `docs/hardware/fritzing/README.md` with the following content:

```markdown
# Fritzing Diagrams — Source Files

This directory contains Fritzing `.fzz` source files and exported PNG images for all
wiring diagrams in the backyard farm platform hardware documentation.

## Files

| File | Type | Used In | Status |
|------|------|---------|--------|
| garden-node.fzz | Fritzing source | [garden-node.md](../garden-node.md) | Pending — author must create |
| garden-node.png | PNG export | [garden-node.md](../garden-node.md) | Pending — export from .fzz |
| coop-node.fzz | Fritzing source | [coop-node.md](../coop-node.md) | Pending — author must create |
| coop-node.png | PNG export | [coop-node.md](../coop-node.md) | Pending — export from .fzz |
| irrigation-relay.fzz | Fritzing source | [irrigation.md](../irrigation.md) | Pending — author must create |
| irrigation-relay.png | PNG export | [irrigation.md](../irrigation.md) | Pending — export from .fzz |
| power-distribution.fzz | Fritzing source | [power.md](../power.md) | Pending — author must create |
| power-distribution.png | PNG export | [power.md](../power.md) | Pending — export from .fzz |

## Why Both .fzz and .png?

The `.fzz` files are the **editable source**. The `.png` files are **exported images** that
are embedded in the Markdown documentation for easy viewing on GitHub. Both must be committed:

- Without `.fzz`: Future contributors cannot update diagrams when hardware changes
- Without `.png`: The wiring diagrams will not display in the Markdown documentation

## Creating Diagrams

See [../README.md](../README.md) for the complete Fritzing workflow.

In summary:
1. Create the breadboard view in Fritzing 1.0.6
2. Save as `[name].fzz` in this directory
3. Export as PNG at 150 DPI to `[name].png` in this directory
4. Commit both files
5. Update the "Status" column in the table above

## Important: Breadboard View Only

Always use **Breadboard view** when creating diagrams for this documentation. Do not use
Schematic view or PCB view — the target reader has no electronics background and cannot
read schematic symbols.

## Diagram Contents

### garden-node.fzz
Shows: Raspberry Pi Zero 2W → STEMMA Soil Sensor (I2C 0x36) → ADS1115 ADC (I2C 0x48) →
DS18B20 temperature probe (1-Wire GPIO4) → DFRobot pH probe (via ADS1115 channel A0)

### coop-node.fzz
Shows: Raspberry Pi 5 → L298N motor driver → 12V linear actuator → limit switches
(GPIO input) → HX711 load cells (GPIO bit-bang, 2× pairs) → DS18B20 (1-Wire GPIO4) →
float switch (GPIO input) → 4-channel relay board

### irrigation-relay.fzz
Shows: Raspberry Pi Zero 2W GPIO → 4-channel optocoupler relay board → 12V solenoid valve
NC/COM wiring → 12V power rail. Includes active-HIGH vs active-LOW trigger annotation.

### power-distribution.fzz
Shows: AC mains → 12V 5A IP65 power supply → DC distribution rail → LM2596 buck converter
(12V→5V for Pi Zero) → relay board 12V input → solenoid valve 12V feed.
Includes IP65 enclosure cable gland entry points.
```

Then create placeholder text files for each of the 4 diagrams. These are plain text files
with a clear "PLACEHOLDER" marker so git tracks the paths while the author creates real diagrams:

Create `docs/hardware/fritzing/garden-node.fzz` with content:
```
PLACEHOLDER — Create this Fritzing diagram using Fritzing 1.0.6
See docs/hardware/fritzing/README.md for instructions.
Diagram: Raspberry Pi Zero 2W + STEMMA Soil Sensor + ADS1115 + DS18B20 + DFRobot pH probe
```

Create `docs/hardware/fritzing/coop-node.fzz` with content:
```
PLACEHOLDER — Create this Fritzing diagram using Fritzing 1.0.6
See docs/hardware/fritzing/README.md for instructions.
Diagram: Raspberry Pi 5 + L298N + linear actuator + limit switches + HX711 load cells + float switch + relay
```

Create `docs/hardware/fritzing/irrigation-relay.fzz` with content:
```
PLACEHOLDER — Create this Fritzing diagram using Fritzing 1.0.6
See docs/hardware/fritzing/README.md for instructions.
Diagram: Raspberry Pi GPIO → 4-channel optocoupler relay → 12V solenoid valve (NC wiring)
```

Create `docs/hardware/fritzing/power-distribution.fzz` with content:
```
PLACEHOLDER — Create this Fritzing diagram using Fritzing 1.0.6
See docs/hardware/fritzing/README.md for instructions.
Diagram: 12V IP65 power supply → LM2596 buck converter → Pi Zero 2W + relay board + solenoid valve
```

Create placeholder PNG files. Since the actual Fritzing diagrams require the Fritzing
application, create minimal valid PNG placeholder files. Use Python to create 1×1 pixel
PNG files as placeholders (any image format viewer will open them):

```bash
python3 -c "
import struct, zlib

def make_placeholder_png(filename, label):
    # Minimal 1x1 white PNG
    def chunk(tag, data):
        c = struct.pack('>I', len(data)) + tag + data
        return c + struct.pack('>I', zlib.crc32(tag + data) & 0xffffffff)
    
    signature = b'\\x89PNG\\r\\n\\x1a\\n'
    ihdr = chunk(b'IHDR', struct.pack('>IIBBBBB', 1, 1, 8, 2, 0, 0, 0))
    # RGB white pixel
    raw = b'\\x00\\xff\\xff\\xff'
    idat = chunk(b'IDAT', zlib.compress(raw))
    iend = chunk(b'IEND', b'')
    with open(filename, 'wb') as f:
        f.write(signature + ihdr + idat + iend)
    print(f'Created placeholder: {filename}')

make_placeholder_png('docs/hardware/fritzing/garden-node.png', 'garden-node')
make_placeholder_png('docs/hardware/fritzing/coop-node.png', 'coop-node')
make_placeholder_png('docs/hardware/fritzing/irrigation-relay.png', 'irrigation-relay')
make_placeholder_png('docs/hardware/fritzing/power-distribution.png', 'power-distribution')
"
```

Note: If Python PNG creation fails for any reason, create the PNG files using a simple
`echo "PLACEHOLDER" > docs/hardware/fritzing/[name].png` approach. The critical requirement
is that the file exists at the correct path so the Markdown `![](fritzing/name.png)` 
references don't produce 404 errors before real diagrams are created.
  </action>
  <verify>
    <automated>ls docs/hardware/fritzing/ | grep -E "(garden-node|coop-node|irrigation-relay|power-distribution)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - docs/hardware/fritzing/README.md exists with files table and workflow description
    - docs/hardware/fritzing/garden-node.fzz exists with PLACEHOLDER content
    - docs/hardware/fritzing/coop-node.fzz exists with PLACEHOLDER content
    - docs/hardware/fritzing/irrigation-relay.fzz exists with PLACEHOLDER content
    - docs/hardware/fritzing/power-distribution.fzz exists with PLACEHOLDER content
    - docs/hardware/fritzing/garden-node.png exists (placeholder image)
    - docs/hardware/fritzing/coop-node.png exists (placeholder image)
    - docs/hardware/fritzing/irrigation-relay.png exists (placeholder image)
    - docs/hardware/fritzing/power-distribution.png exists (placeholder image)
    - ls docs/hardware/fritzing/ returns at least 9 files (README + 4×fzz + 4×png)
    - grep "PLACEHOLDER" docs/hardware/fritzing/garden-node.fzz returns match
  </acceptance_criteria>
  <done>docs/hardware/fritzing/ directory exists with README, 4 placeholder .fzz files, and 4 placeholder PNG files. All Markdown diagram references in Plans 03–05 will resolve without broken images.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation only | No application code, no network boundaries, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Placeholder .fzz files | accept | Files are plaintext placeholders tracked in git; version history provides evidence if changed. No sensitive content |
| T-06-05 | Information Disclosure | README.md Fritzing instructions | accept | Public workflow documentation for open-source tooling; no credentials or private data |
</threat_model>

<verification>
After task completion:

1. `docs/hardware/README.md` exists and links all 6 subsystem docs
2. `docs/hardware/fritzing/README.md` exists with workflow instructions
3. `ls docs/hardware/fritzing/` shows 9+ files (README.md + 4 .fzz + 4 .png)
4. grep "PLACEHOLDER" docs/hardware/fritzing/garden-node.fzz succeeds
5. All 4 PNG placeholder files are valid files (file size > 0 bytes)
</verification>

<success_criteria>
Directory structure is established. Plans 03, 04, and 05 can write subsystem docs and reference `fritzing/[name].png` without broken images. The Fritzing workflow is documented so the author knows exactly what to create after Phase 6 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardware-shopping-list-and-wiring-diagrams/06-02-SUMMARY.md`
</output>
