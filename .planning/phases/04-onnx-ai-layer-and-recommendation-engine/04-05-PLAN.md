---
phase: 04-onnx-ai-layer-and-recommendation-engine
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified: []
autonomous: false
requirements:
  - AI-03
  - AI-06
  - AI-07

must_haves:
  truths:
    - "All Phase 4 backend and frontend components integrate end-to-end"
    - "AI Status card renders on Home tab with correct maturity state"
    - "Settings page toggles work and persist across page reload"
    - "Recommendation queue shows source badge on cards"
    - "Bridge starts without errors with inference components loaded"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete Phase 4 integration. Verify that all backend inference components (scheduler, watcher, toggle, maturity tracking) and frontend UI (AIStatusCard, settings page, source badges) work together end-to-end.

Purpose: Before marking Phase 4 complete, a human confirms the visible behavior matches the AI-03, AI-06, and AI-07 requirements. This is the single verification point for the entire phase.

Output: Confirmation that Phase 4 is functionally complete or a list of issues to address.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-onnx-ai-layer-and-recommendation-engine/04-CONTEXT.md
@.planning/phases/04-onnx-ai-layer-and-recommendation-engine/04-03-SUMMARY.md
@.planning/phases/04-onnx-ai-layer-and-recommendation-engine/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all tests and verify build</name>
  <files></files>
  <read_first>
    hub/bridge/requirements.txt,
    hub/bridge/tests/inference/test_feature_aggregator.py,
    hub/bridge/tests/inference/test_inference_service.py,
    hub/bridge/tests/inference/test_maturity_tracker.py
  </read_first>
  <action>
    Run the complete test suite for both backend and frontend:
    1. `cd hub/bridge && python -m pytest tests/ -x -q` -- all bridge tests including inference tests
    2. `cd hub/dashboard && npm test -- --run` -- all dashboard tests including new AI component tests
    3. `cd hub/dashboard && npm run build` -- verify production build succeeds with no errors
    4. Verify key file existence:
       - hub/bridge/inference/feature_aggregator.py
       - hub/bridge/inference/inference_service.py
       - hub/bridge/inference/inference_scheduler.py
       - hub/bridge/inference/model_watcher.py
       - hub/bridge/inference/maturity_tracker.py
       - hub/bridge/inference/ai_settings.py
       - hub/bridge/inference/training/train_zone_health.py
       - hub/bridge/inference/training/train_irrigation.py
       - hub/bridge/inference/training/train_flock_anomaly.py
       - hub/bridge/inference/synthetic/generate_synthetic_data.py
       - hub/api/inference_settings_router.py
       - hub/dashboard/src/lib/AIStatusCard.svelte
       - hub/dashboard/src/lib/DomainMaturityRow.svelte
       - hub/dashboard/src/lib/AISettingsToggle.svelte
       - hub/dashboard/src/routes/settings/ai/+page.svelte
    5. Verify GOOD-flag enforcement: `grep -r "quality = 'GOOD'" hub/bridge/inference/` should match in feature_aggregator.py and all training scripts.
    6. Verify recommendation compatibility: `grep "source.*ai" hub/bridge/inference/inference_service.py` confirms source field is set.
    Report pass/fail for each step.
  </action>
  <verify>
    <automated>cd /Users/patrick.lewis/code/backyard-farm/hub/bridge && python -m pytest tests/ -x -q && cd /Users/patrick.lewis/code/backyard-farm/hub/dashboard && npm test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All Python tests exit 0
    - All JavaScript tests exit 0
    - npm run build exits 0
    - All 16 key files listed above exist
    - grep for "quality = 'GOOD'" matches in feature_aggregator.py and training scripts
  </acceptance_criteria>
  <done>All tests pass, build succeeds, all Phase 4 files exist, GOOD-flag enforcement verified across codebase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 4 integration</name>
  <files></files>
  <action>
    Present the following verification steps to the user. This task was automated by Plans 01-04. The human now confirms visible behavior.

    Complete ONNX AI Layer (Phase 4) was built:
    - Backend: Feature aggregation service, ONNX inference service with hot-reload, three training pipelines (zone health, irrigation, flock anomaly), APScheduler inference scheduler, model watcher, AI/Rules toggle persistence, maturity tracking, synthetic data generator
    - Frontend: AI Status card on Home tab with maturity progress per domain, per-domain AI/Rules toggle settings page at /settings/ai, recommendation source badges (AI/RULES), settings icon in header
    - Integration: Bridge main.py orchestrates all inference components; API proxies toggle and maturity endpoints to bridge

    Verification steps for user:
    1. Start the hub services: `docker compose up -d` (or local dev start command)
    2. Open dashboard in browser at https://farm.local (or localhost)

    Home tab (AI Status Card):
    3. Verify "AI Engine" card appears above flock summary on Home tab
    4. If no sensor data: card should show "0 wk / 4 wk needed" progress for all domains
    5. If synthetic data was generated: verify progress bars reflect actual data weeks
    6. Verify cold-start message: "The AI is still learning -- rule-based recommendations are active."
    7. Verify "Configure AI" link navigates to /settings/ai

    Settings page (/settings/ai):
    8. Click settings gear icon in header -- verify it navigates to /settings/ai
    9. Verify page heading: "AI Engine Settings"
    10. Verify three toggle sections: Irrigation Recommendations, Zone Health Scoring, Flock Anomaly Detection
    11. If models NOT mature: toggles should be disabled (grayed out, tooltip "Model not yet mature")
    12. If models ARE mature: toggle from Rules to AI -- verify toast "AI enabled for ..."
    13. Toggle back to Rules -- verify toast "Rules restored for ..."
    14. Refresh page -- verify toggle state persisted

    Recommendation Queue (/recommendations):
    15. If recommendations exist: verify each card shows "AI" (green) or "RULES" (gray) badge
    16. Verify badge position: inside controls area, before approve/reject buttons

    Mobile layout:
    17. On phone-width viewport (< 640px): verify AI Status card above flock summary
    18. On desktop viewport (>= 640px): verify AI Status card in right column
    19. Verify settings page is usable on phone width

    If synthetic data not seeded: Run `python scripts/generate_synthetic_data.py --weeks 6` first.

    Resume signal: Type "approved" to mark Phase 4 complete, or describe specific issues to fix.
  </action>
  <verify>
    <automated>echo "Human verification -- no automated check. User must type approved or describe issues."</automated>
  </verify>
  <acceptance_criteria>
    - User confirms AI Status card visible on Home tab
    - User confirms settings page toggle behavior
    - User confirms recommendation source badges
    - User confirms mobile layout
    - User types "approved"
  </acceptance_criteria>
  <done>Human has verified all Phase 4 UI and integration behavior end-to-end and typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries -- verification only.

## STRIDE Threat Register

No new threats -- this plan is verification only.
</threat_model>

<verification>
1. All automated tests pass (Python + JS)
2. Production build succeeds
3. Human confirms UI matches 04-UI-SPEC.md
4. Human confirms toggle behavior per D-05, D-06
5. Human confirms source badges on recommendation cards
</verification>

<success_criteria>
- All Phase 4 tests pass and build succeeds
- Human confirms AI Status card visible on Home tab (AI-07)
- Human confirms settings page toggle works (D-05, D-06)
- Human confirms recommendation source badges display correctly
- Human confirms mobile layout is usable
</success_criteria>

<output>
After completion, create `.planning/phases/04-onnx-ai-layer-and-recommendation-engine/04-05-SUMMARY.md`
</output>
