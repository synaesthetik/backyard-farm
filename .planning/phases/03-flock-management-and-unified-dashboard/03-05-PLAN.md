---
phase: 03-flock-management-and-unified-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-06"]
files_modified: []
autonomous: false
requirements:
  - FLOCK-01
  - FLOCK-02
  - FLOCK-03
  - FLOCK-04
  - FLOCK-05
  - FLOCK-06
  - UI-01

must_haves:
  truths:
    - "All Phase 3 features are visually correct and functionally working"
    - "Home tab shows zones and flock summary on one screen"
    - "Coop tab shows egg count, production chart, feed sparkline"
    - "Flock settings form saves and reflects in production model"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for Phase 3. The farmer confirms that the Home tab unified
overview, Coop tab flock data displays, and flock settings all work correctly in the browser.

Purpose: Visual and functional verification that cannot be automated — layout, animation,
chart rendering, and form UX need human eyes.

Output: Approval or issues list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-flock-management-and-unified-dashboard/03-01-SUMMARY.md
@.planning/phases/03-flock-management-and-unified-dashboard/03-02-SUMMARY.md
@.planning/phases/03-flock-management-and-unified-dashboard/03-03-SUMMARY.md
@.planning/phases/03-flock-management-and-unified-dashboard/03-04-SUMMARY.md
@.planning/phases/03-flock-management-and-unified-dashboard/03-06-SUMMARY.md
</context>

<threat_model>
| Threat | Severity | Mitigation | Residual Risk |
|--------|----------|------------|---------------|
| N/A | N/A | Checkpoint plan — no code changes | N/A |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 3 flock management and unified dashboard</name>
  <files>none — verification only</files>
  <read_first>
    .planning/phases/03-flock-management-and-unified-dashboard/03-01-SUMMARY.md,
    .planning/phases/03-flock-management-and-unified-dashboard/03-02-SUMMARY.md,
    .planning/phases/03-flock-management-and-unified-dashboard/03-03-SUMMARY.md,
    .planning/phases/03-flock-management-and-unified-dashboard/03-04-SUMMARY.md,
    .planning/phases/03-flock-management-and-unified-dashboard/03-06-SUMMARY.md
  </read_first>
  <action>
    Present the following verification checklist to the user and wait for their response.

    Phase 3 complete: flock management backend (egg estimation, production model, feed
    consumption, alerts), flock REST API and WebSocket snapshot, unified Home tab, extended
    Coop tab with egg count / production chart / feed sparkline / hen indicator, and flock
    settings page.

    Start the system: `docker compose up -d` from hub/

    **1. Home Tab (UI-01)**
    - Open the dashboard in a browser. It should land on the Home tab at `/`.
    - Verify 4 tabs visible: Home, Zones, Coop, Recs.
    - Verify compact zone cards show health badge + zone name + moisture.
    - Verify FlockSummaryCard shows door status, egg count (or "--"), trend indicator.
    - Tap a zone card — should navigate to /zones/[id].
    - Tap the flock summary card — should navigate to /coop.
    - Verify the Home tab is highlighted in the tab bar when at /.
    - Navigate to /zones — verify the Zones tab is highlighted and zones list renders.

    **2. Coop Tab — Egg Count**
    - Navigate to /coop.
    - Verify "Eggs Today" section is at the top with egg count display.
    - If no sensor data, verify "--" placeholder shows.
    - Verify Refresh button is present and has correct styling.
    - Tap Refresh — verify the icon spins briefly and the button disables during request.

    **3. Coop Tab — Hen Present Indicator**
    - When hen_present is true (may need to simulate), verify the Egg icon pulses with the
      green animation and "Hen present" text is visible.
    - When hen_present is false, verify the indicator is completely hidden (not just invisible).

    **4. Coop Tab — Production Chart**
    - Verify "Egg Production — 30 Days" section heading (em dash, not hyphen).
    - If fewer than 3 days of data, verify the "Not enough data yet" message.
    - If data exists, verify two-series chart: solid green (Actual) and dashed gray (Expected).
    - Verify chart is responsive (resize browser width).

    **5. Coop Tab — Feed Consumption**
    - Below the feed level bar, verify "~Ng/day" rate label with sparkline.
    - If no data, verify the section is hidden (not showing empty chart).
    - Verify sparkline is a small green polyline, approximately 80x24px.

    **6. Flock Settings**
    - Tap the gear icon on the Coop tab header — should navigate to /coop/settings.
    - Verify "Flock Settings" heading.
    - Verify breed dropdown with 10+ breeds.
    - Select "Custom" — verify lay rate input appears.
    - Select a non-Custom breed — verify lay rate input disappears.
    - Verify all form fields have labels and hint text.
    - Try saving with flock size = 0 — verify inline validation error.
    - Fill valid values and save — verify navigation back to /coop.

    **7. Alerts**
    - If production drop or feed consumption drop alerts are firing, verify they appear
      in the persistent alert bar as P1 (amber) alerts.

    **8. Mobile View**
    - Resize to phone width (~375px).
    - Verify Home tab: FlockSummaryCard at top, zone cards below in single column.
    - Verify Coop tab: all sections stack vertically and are readable.
    - Verify tab bar: all 4 tabs fit without text wrapping.

    Resume signal: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>echo "Checkpoint — awaiting human verification"</automated>
  </verify>
  <acceptance_criteria>
    - Home tab renders at / with compact zones and flock summary
    - TabBar shows 4 tabs with correct active states
    - Coop tab shows egg count, hen indicator, production chart, feed sparkline
    - Flock settings form works end-to-end (load, edit, validate, save)
    - Layout works on both mobile and desktop widths
    - No console errors in browser dev tools
  </acceptance_criteria>
  <done>User has approved all Phase 3 features as visually and functionally correct.</done>
</task>

</tasks>

<verification>
Human verification — no automated checks needed.
</verification>

<success_criteria>
- User confirms all Phase 3 features work correctly
- No visual or functional issues requiring gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/03-flock-management-and-unified-dashboard/03-05-SUMMARY.md`
</output>
