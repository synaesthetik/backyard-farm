---
phase: 03-flock-management-and-unified-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-06"]
files_modified:
  - hub/dashboard/src/lib/CoopPanel.svelte
  - hub/dashboard/src/lib/HenPresentIndicator.svelte
  - hub/dashboard/src/lib/ProductionChart.svelte
  - hub/dashboard/src/lib/FeedSparkline.svelte
  - hub/dashboard/src/lib/ProductionChart.test.ts
autonomous: true
requirements:
  - FLOCK-01
  - FLOCK-03
  - FLOCK-06

must_haves:
  truths:
    - "Coop tab shows today's egg count estimated from nesting box weight sensor"
    - "Hen present indicator with pulsing egg icon appears when hen is in nesting box"
    - "30-day production trend chart shows actual vs expected as two-series overlay"
    - "Feed consumption rate displayed as daily rate with 7-day inline sparkline"
    - "Refresh button triggers immediate re-poll of nesting box sensor"
  artifacts:
    - path: "hub/dashboard/src/lib/HenPresentIndicator.svelte"
      provides: "Animated hen detection indicator"
      contains: "hen-pulse"
    - path: "hub/dashboard/src/lib/ProductionChart.svelte"
      provides: "uPlot two-series production overlay chart"
      contains: "uPlot"
    - path: "hub/dashboard/src/lib/FeedSparkline.svelte"
      provides: "Inline SVG 7-day sparkline"
      contains: "polyline"
    - path: "hub/dashboard/src/lib/CoopPanel.svelte"
      provides: "Extended with egg count, hen indicator, production chart, feed sparkline"
      contains: "HenPresentIndicator"
  key_links:
    - from: "hub/dashboard/src/lib/CoopPanel.svelte"
      to: "hub/dashboard/src/lib/ws.svelte.ts"
      via: "reads dashboardStore.eggCount and dashboardStore.feedConsumption"
      pattern: "dashboardStore\\.eggCount"
    - from: "hub/dashboard/src/lib/CoopPanel.svelte"
      to: "/api/flock/refresh-eggs"
      via: "Refresh button POSTs to trigger immediate estimation"
      pattern: "fetch.*refresh-eggs"
    - from: "hub/dashboard/src/lib/CoopPanel.svelte"
      to: "/api/flock/egg-history"
      via: "ProductionChart fetches 30-day history"
      pattern: "fetch.*egg-history"
---

<objective>
Extend the Coop tab with the Phase 3 flock data displays: egg count section with refresh
button, animated hen present indicator, 30-day production trend chart (uPlot overlay),
and feed consumption rate with inline SVG sparkline.

Purpose: This transforms the Coop tab from door-and-feed-only into the complete flock health
picture. The farmer sees egg production, hen activity, and feed trends all in one place.

Output: HenPresentIndicator, ProductionChart, FeedSparkline components; extended CoopPanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-flock-management-and-unified-dashboard/03-CONTEXT.md
@.planning/phases/03-flock-management-and-unified-dashboard/03-UI-SPEC.md
@.planning/phases/03-flock-management-and-unified-dashboard/03-01-SUMMARY.md
@.planning/phases/03-flock-management-and-unified-dashboard/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (backend) -->
API endpoints created in Plan 01:
- GET /api/flock/egg-history?days=30 -> { dates: string[], actual: number[], expected: number[] }
- POST /api/flock/refresh-eggs -> { estimated_count: number, hen_present: boolean, raw_weight_grams: number }

<!-- From Plan 02 (WS store extensions) -->
From hub/dashboard/src/lib/ws.svelte.ts (extended in Plan 02):
```typescript
class DashboardStore {
  eggCount = $state<{ today: number; hen_present: boolean; updated_at: string } | null>(null);
  feedConsumption = $state<{ rate_grams_per_day: number; weekly: number[] } | null>(null);
  feedLevel = $state<{ percentage: number; below_threshold: boolean } | null>(null);
  // ... existing fields
}
```

From hub/dashboard/src/lib/types.ts (extended in Plan 02):
```typescript
export interface NestingBoxDelta { type: 'nesting_box'; estimated_count: number; hen_present: boolean; raw_weight_grams: number; updated_at: string; }
export interface FeedConsumptionDelta { type: 'feed_consumption'; rate_grams_per_day: number; weekly: number[]; }
```

<!-- Existing SensorChart pattern to follow for ProductionChart -->
From hub/dashboard/src/lib/SensorChart.svelte:
```svelte
<!-- Pattern: onMount -> fetch data -> requestAnimationFrame -> new uPlot(opts, data, container) -->
<!-- ResizeObserver for responsive width -->
<!-- $effect rune for data updates -->
```

From hub/dashboard/src/lib/CoopPanel.svelte:
```svelte
<!-- Current sections: door status, schedule, feed level bar, water level bar -->
<!-- Uses dashboardStore for door state, feed level, water level, coop schedule -->
```
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation | Residual Risk |
|--------|----------|------------|---------------|
| XSS via egg-history API response in chart | LOW | uPlot renders to canvas (not DOM), numeric data only | NEGLIGIBLE |
| Refresh button spam overloading bridge | LOW | Disable button during in-flight; single-user system; bridge handles one request at a time | LOW |
| uPlot renders at 0px width on hidden tab | MEDIUM | Use {#if} conditional rendering (not CSS hidden) per UI-SPEC and research Pitfall 2; defer uPlot instantiation until Coop tab is active | LOW |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: HenPresentIndicator, ProductionChart, and FeedSparkline components</name>
  <files>
    hub/dashboard/src/lib/HenPresentIndicator.svelte,
    hub/dashboard/src/lib/ProductionChart.svelte,
    hub/dashboard/src/lib/FeedSparkline.svelte,
    hub/dashboard/src/lib/ProductionChart.test.ts
  </files>
  <read_first>
    hub/dashboard/src/lib/SensorChart.svelte,
    hub/dashboard/src/lib/ws.svelte.ts,
    hub/dashboard/src/lib/types.ts,
    hub/dashboard/src/app.css,
    .planning/phases/03-flock-management-and-unified-dashboard/03-UI-SPEC.md
  </read_first>
  <action>
    **HenPresentIndicator (hub/dashboard/src/lib/HenPresentIndicator.svelte):** Per UI-SPEC HenPresentIndicator section.
    - Render only when `hen_present = true` (caller uses `{#if}` to conditionally render this component).
    - Layout: inline flex row. Align-items: center. Gap: 4px (var(--spacing-xs)).
    - Contents: Lucide `Egg` icon at 16px in `var(--color-accent)`, with `aria-hidden="true"`. Text "Hen present" in 16px/600, `var(--color-accent)`.
    - Animation: `@keyframes hen-pulse` per UI-SPEC: scale(1) -> scale(1.15)/opacity(0.7) -> scale(1) on 2s ease-in-out infinite loop. Apply to the Egg icon element.
    - `aria-live="polite"` on the container div. Screen readers announce "Hen present" when it appears.

    **ProductionChart (hub/dashboard/src/lib/ProductionChart.svelte):** Per UI-SPEC ProductionChart section and research Pattern 4.
    - Follow the exact SensorChart.svelte pattern: `onMount` + `requestAnimationFrame` + `ResizeObserver`.
    - No time range toggle — always 30 days.
    - On mount, fetch from `/api/flock/egg-history?days=30`. Response shape: `{ dates: string[], actual: number[], expected: number[] }`.
    - Convert dates to Unix timestamps for uPlot x-axis.
    - Build uPlot options per research Pattern 4:
      - Series[0]: {} (x-axis timestamps)
      - Series[1]: { label: 'Actual', stroke: '#4ade80', width: 1.5 } — solid green
      - Series[2]: { label: 'Expected', stroke: '#94a3b8', width: 1.5, dash: [4, 4] } — dashed secondary
      - Axes: stroke '#94a3b8', grid stroke '#2d3149', size 14 (minimum per UI-SPEC typography constraint).
      - Cursor: stroke '#6b7280'.
      - No fill under either series.
    - Height: 160px on mobile, 200px on desktop (>= 640px). Use CSS media query or ResizeObserver.
    - Background: `var(--color-bg)` on the container.
    - Loading state: skeleton div at chart dimensions, `var(--color-surface)` background.
    - Empty state (< 3 days data): skeleton + "Not enough data yet \u2014 check back in a few days" centered (14px/400, --color-muted).
    - `aria-label="Egg production chart \u2014 actual vs expected, last 30 days"` on the container.
    - **Pitfall 2:** This component is inside CoopPanel which is only visible when Coop tab is active. Use `{#if}` conditional rendering in CoopPanel (not CSS hidden) so uPlot instantiates only when visible.

    **FeedSparkline (hub/dashboard/src/lib/FeedSparkline.svelte):** Per UI-SPEC FeedSparkline section and research Pattern 5.
    - Dimensions: 80px wide x 24px tall. Fixed SVG viewBox.
    - Accepts `values: number[]` prop (7 daily consumption values, oldest first).
    - If fewer than 2 data points, render nothing (`{#if values.length >= 2}`).
    - Compute polyline points using `toPolylinePoints()`:
      ```typescript
      function toPolylinePoints(values: number[], width = 80, height = 24): string {
        const min = Math.min(...values);
        const max = Math.max(...values);
        const range = max - min || 1;  // division-by-zero guard
        return values.map((v, i) => {
          const x = (i / (values.length - 1)) * width;
          const y = height - ((v - min) / range) * height;
          return `${x},${y}`;
        }).join(' ');
      }
      ```
    - Single `<polyline>` element. `fill="none"`, `stroke="var(--color-accent)"`, `stroke-width="1.5"`.
    - `aria-hidden="true"` on the SVG — decorative only.

    **ProductionChart test (hub/dashboard/src/lib/ProductionChart.test.ts):**
    - Test: renders loading skeleton initially
    - Test: renders empty state message when data has < 3 days
    - Test: has correct aria-label
    - Note: uPlot canvas testing is limited; focus on state transitions and accessibility attributes.
  </action>
  <verify>
    <automated>cd /Users/patrick.lewis/code/backyard-farm/hub/dashboard && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - hub/dashboard/src/lib/HenPresentIndicator.svelte contains hen-pulse keyframes, Egg icon, aria-live="polite"
    - hub/dashboard/src/lib/ProductionChart.svelte uses uPlot with 2 series (Actual/Expected), fetches /api/flock/egg-history
    - hub/dashboard/src/lib/ProductionChart.svelte has empty state "Not enough data yet" and loading skeleton
    - hub/dashboard/src/lib/FeedSparkline.svelte renders SVG polyline from values prop with aria-hidden="true"
    - hub/dashboard/src/lib/FeedSparkline.svelte guards against < 2 data points and division by zero
    - ProductionChart.test.ts passes
  </acceptance_criteria>
  <done>All three new display components created with correct visual and accessibility attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend CoopPanel with egg section, production chart, feed sparkline, and refresh</name>
  <files>
    hub/dashboard/src/lib/CoopPanel.svelte
  </files>
  <read_first>
    hub/dashboard/src/lib/CoopPanel.svelte,
    hub/dashboard/src/lib/HenPresentIndicator.svelte,
    hub/dashboard/src/lib/ProductionChart.svelte,
    hub/dashboard/src/lib/FeedSparkline.svelte,
    hub/dashboard/src/lib/ws.svelte.ts,
    hub/dashboard/src/lib/CommandButton.svelte,
    .planning/phases/03-flock-management-and-unified-dashboard/03-UI-SPEC.md
  </read_first>
  <action>
    **Extend CoopPanel (hub/dashboard/src/lib/CoopPanel.svelte):** Per UI-SPEC Extended CoopPanel section.

    Import new components: HenPresentIndicator, ProductionChart, FeedSparkline.
    Import `RefreshCw` and `Settings` from lucide-svelte.
    Import `goto` from '$app/navigation'.

    **Updated section order (top to bottom):**

    1. **Header row:** Add settings gear icon button to the right of the page heading area.
       - Lucide `Settings` icon at 24px. `aria-label="Flock settings"`.
       - On click/tap: `goto('/coop/settings')`.
       - Positioned right-aligned in the Coop page header row.

    2. **Egg count section (NEW):** Insert ABOVE the existing door status section per UI-SPEC (egg section is the new focal point).
       - Section heading: "Eggs Today" (20px/600).
       - Flex row (space-between):
         - Left: egg count display. Read `dashboardStore.eggCount?.today`. Display in 28px/600, --color-text-primary, tabular-nums. Format: "{N} eggs". If null: "--" in --color-muted italic.
         - Right: Refresh button. Label "Refresh" (16px/400). Lucide `RefreshCw` icon 16px at left of label. Background --color-surface, border --color-border, text --color-text-primary. Touch target: 44px min-height.
       - Below count: `{#if dashboardStore.eggCount?.hen_present}<HenPresentIndicator />{/if}`
       - Refresh button behavior: on click, POST to `/api/flock/refresh-eggs`. Set `refreshing = true` state ($state). While refreshing: RefreshCw icon spins (CSS `animation: spin 1s linear infinite`), button disabled with `aria-disabled="true"` and `aria-busy="true"`, `aria-label="Refresh egg count"`. On response, update is handled via WebSocket delta (not from fetch response), so just set refreshing=false. On error, dispatch `farm:toast` error event.

    3. **Production chart section (NEW):**
       - Section heading: "Egg Production \u2014 30 Days" (20px/600). Use em dash, not hyphen.
       - `<ProductionChart />` — full-width. Wrap in `{#if}` so it only renders when Coop tab is visible (it's already only rendered when this route is active via SvelteKit routing, so no extra guard needed unless CoopPanel is imported elsewhere).

    4. **Door status section** — unchanged from Phase 2.

    5. **Schedule display** — unchanged from Phase 2.

    6. **Feed level section** — unchanged from Phase 2.

    7. **Feed consumption section (NEW):** Insert directly below the existing feed level bar.
       - No separate section heading per UI-SPEC copywriting contract.
       - Flex row, align-items: center, gap: 8px (sm):
         - Left: rate label "~{N}g/day" (14px/400, --color-text-secondary). Read `dashboardStore.feedConsumption?.rate_grams_per_day`. Round to integer. If null: hide entire section.
         - Right: `<FeedSparkline values={dashboardStore.feedConsumption?.weekly ?? []} />`

    8. **Water level section** — unchanged from Phase 2.

    **Style additions:** Add CSS for the refresh button spin animation (same pattern as CommandButton spinner). Add section gap of 24px (lg) between sections per UI-SPEC spacing.
  </action>
  <verify>
    <automated>cd /Users/patrick.lewis/code/backyard-farm/hub/dashboard && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CoopPanel imports and renders HenPresentIndicator, ProductionChart, FeedSparkline
    - CoopPanel has "Eggs Today" section heading with egg count display from dashboardStore.eggCount
    - CoopPanel has Refresh button that POSTs to /api/flock/refresh-eggs with spin animation
    - CoopPanel has "Egg Production — 30 Days" section with ProductionChart
    - CoopPanel has feed consumption rate label + FeedSparkline below feed level bar
    - CoopPanel has Settings gear icon linking to /coop/settings with aria-label="Flock settings"
    - HenPresentIndicator renders conditionally when hen_present is true
    - Section order: eggs -> production chart -> door -> schedule -> feed level -> feed consumption -> water
    - Existing dashboard tests pass
  </acceptance_criteria>
  <done>CoopPanel fully extended with egg count, hen indicator, production chart, feed consumption sparkline, refresh button, and settings link. All Phase 3 display data visible on Coop tab.</done>
</task>

</tasks>

<verification>
- Dashboard builds: `cd hub/dashboard && npm run build`
- All tests pass: `cd hub/dashboard && npx vitest run`
- CoopPanel imports new components: `grep "HenPresentIndicator\|ProductionChart\|FeedSparkline" hub/dashboard/src/lib/CoopPanel.svelte`
- Refresh button exists: `grep "refresh-eggs" hub/dashboard/src/lib/CoopPanel.svelte`
- Hen indicator has animation: `grep "hen-pulse" hub/dashboard/src/lib/HenPresentIndicator.svelte`
</verification>

<success_criteria>
- Coop tab shows egg count section at top with count, refresh button, and conditional hen indicator
- 30-day production chart renders with actual (green solid) and expected (gray dashed) series
- Feed consumption rate + sparkline renders below feed level bar
- Settings gear icon navigates to /coop/settings
- All existing and new dashboard tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-flock-management-and-unified-dashboard/03-03-SUMMARY.md`
</output>
